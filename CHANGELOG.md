# 迁移进度 Changelog

按切片记录 Esper 9.0.0 → Go 的迁移明细；[README](README.md) 只保留移植概况与大概进度。每完成一个切片在此追加记录，并同步 `compat/capability-manifest.json` 与 [实施规划](docs/esper-go-port-implementation-plan.md)。历史条目由 README 迁入，保持原始行文与顺序。

## 2026-08-09

本轮开启 InfraNamedWindowViews 切片（12/58 execution）：新增 infra_namedwindow_views_parity_test.go 十二个对照，覆盖 named window 数据窗口保留矩阵——KeepAllSimple（keepall 持有完整 SupportBean 事件，insert-into select * 以 OnEvent+InsertIntoNamedWindow 全列赋值等价表达）、KeepAllSceneTwo（keepall + on-delete 触发器 + 逐步迭代器断言）、LengthWindow/LengthWindowSceneTwo（length(3) 满窗淘汰最老事件的 IR pair 在窗口 listener 与 irstream 消费语句两侧同断言，on-delete 中途删除）、LastEvent/LastEventSceneTwo（lastevent 替换产生 IR pair、on-delete 清空）、FirstEvent（state.go 新增 FirstEventWindowSpec 保留支持：窗口占用后插入被静默丢弃且完全不触发 listener 回调，普通插入路径与 merge 插入路径同步补齐，对应 Esper firstevent 语义）、Unique/UniqueSceneTwo（unique(key) 重复键替换 IR pair，快照按首见键序对应 Java any-order 断言）、FirstUnique（firstunique(key) 重复键静默丢弃）、SortWindow/SortWindowSceneTwo（sort(3, value asc) 迭代器按排序序返回，满窗淘汰最高值 IR pair，同值 tie 时新事件排在旧事件前——reverseSortedEqualRuns，排序超出保留大小的插入在同一 delta 内自增自删产生与自身的 IR pair）。窗口迭代器状态以 NamedWindow.Snapshot 按保留序读取对应 Java assertPropsPerRowIterator。新增 12 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；timewindow/exttime/timeorder/timeaccum/timebatch/timelengthbatch 时间视图、lengthbatch/firstlength、per-group、bean-backed/contained、intersection、filtering consumer、late consumer、pattern、TTL 与 invalid 矩阵仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过（含 InfraNamedWindowViews 58 executions）。Coverage 1493/4140 (36.06%). All gates pass.

本轮关闭 EPLSubselectUnfiltered 切片（19/19 execution，18 个 Go 对照）：新增 epl_subselect_unfiltered_parity_test.go 覆盖无过滤标量子查询全矩阵——UnlimitedStream/LengthWindow（tryAssertMultiRowUnfiltered 多行窗口场景，标量子查询多行命中按 SubqueryNullOnMultiple 返回 null）、AsAfterSubselect/WithAsWithinSubselect/NoAs（As 命名在子查询外/内/缺失三种形态）、LastEvent（lastevent 窗口单行场景 tryAssertSingleRowUnfiltered）、Expression（子查询投影为字符串 concat）、TwoSubqSelect（同 select 两个计算子查询）、WhereClauseReturningTrue/WhereClauseWithExpression（子查询内 where 常量真/表达式谓词）、ComputedResult（对标量子查询结果做外层算术且 null 穿透传播）、FilterInside（子查询内流过滤器）、CustomFunction（Func1("minusOne") 自定义函数参与子查询谓词）、SelfSubselect（insert-into count 流经 lastevent 子查询回读，只能看到此前已路由事件的计数而非在途触发事件）、StartStopStatement（常量真子查询门控外层流，undeploy/redeploy 重置子查询窗口状态）、JoinUnfiltered（S0×S1 keepall join 内两个无相关无过滤子查询，多行命中返回 null，末轮 3 行累积以 listener.allRows 断言）、StreamPrior（OM/Compile 变体共享运行时语义同测，Esper prior(0, x) 为候选行自身按约定映射为行字段、prior(N, x) 映射为 Go Prior(N-1, x)）。plan.go 的 validateSubquery 新增两项 Esper 校验：标量投影内嵌套子查询拒绝（subquery-within-subquery；Go 保留 SubqueryExists 谓词嵌套与 SubqueryRow 多列 fragment 两个已登记扩展，故仅收窄到 scalar projection 形态）与子查询 filter 内聚合函数拒绝（aggregation functions are not supported within subquery filters）；InvalidSubselect 对照断言 windowless select/filter、invalid property、nested、aggregate-in-filter、non-bool predicate 六类 Build 拒绝，unqualified-property 与 IN-wildcard-type-mismatch 在 Go 类型化 builder 下编译期不可能（approved difference），group-by/order-by 内子查询拒绝仍为后续切片。新增 19 个 Java runtime 已登记到 case.epl-subselect-unfiltered 与 epl.subselect.unfiltered（status partial）。Java TestSuiteEPLSubselect 19/19 通过。Coverage 1481/4140 (35.77%). All gates pass.

本轮关闭 EPLSubselectFiltered 切片（累计 26/27 execution，新增 12 个对照）：多流外层 join 相关（Joined2Streams/3Streams/3SceneTwo 以 JoinField(source, name) 引用外层 join tuple 的 s1.p10/s2.p20/s3.p30；Joined4Coercion/4BackCoercion 覆盖 int/long/double 属性跨流数值 coercion，EqualOf 混合类型相等，三种与两种 AND 操作数排序变体同测）；JoinFilteredOne/Two 覆盖 join where 中的标量子查询（p00||p10 concat 与 EqualOf 比较、where 整体为布尔投影子查询）与 select 中的 prior/prev 子查询（Esper prior(1, x) 按既有约定映射为 Go Prior(0, x)，prev(1, x) 按窗口历史）；MultikeyWArrayPrimitive/2Field/Composite 以 Is 的 null 安全深比较表达 int 数组内容相关键（null 数组匹配 null 数组、多行命中返回 null，SubqueryNullOnMultiple）；SubselectMixMax 以 SortWindow(1, Descending/Ascending(measurement)) 承载 sort 窗口 wildcard 子查询返回最高/最低事件负载；SubselectPrior 落地 insert-into 链对照——Join 两路 device 过滤 lastevent 流按 type 相等后 SelectLeft/SelectRight 以 JoinEventValue 插入 Pair map 流，消费端 FromAny("Pair") 过滤器以 Coalesce(SubqueryValue(pdrLast), -1) 对比目标流 lastevent 子查询去重（Esper wildcard join fragment 属性 a.id 由新增的 NestedField(Event, name) 表达式导航插入的 Event 值）。新增 expr.go 的 NestedField 能力并登记 12 个 Java runtime 到 case.epl-subselect-filtered 与 epl.subselect.filtered；套件仅剩 SelectWildcardNoName（subselect_1 自动命名为 EPL 语法糖，Go 要求显式别名，approved difference）。Java TestSuiteEPLSubselect 19/19 通过。Coverage 1462/4140 (35.31%). All gates pass.

本轮开启 EPLSubselectFiltered 切片（14/27 execution）：修正子查询 having 语义——Esper 允许非聚合、无 group-by 的 having，在 where 之后逐行过滤（subquery.go 非聚合候选路径补 having 求值，plan.go 移除「having 需要聚合投影或 group-by」的校验拒绝，原拒绝断言改写为接受断言）；新增 epl_subselect_filtered_parity_test.go 十个对照：HavingNoAggNoFilterNoWhere/WWhere/WFilterWWhere（having 逐行过滤、与 where/流过滤器组合）、SelectWithWhereJoined（OuterField 相关标量子查询 p10=s0.p00）、WhereConstant（单列常量、双列常量、between 范围三场景，多行命中时用 SubqueryNullOnMultiple 对应 Esper 标量子查询多行返回 null）、WherePrevious（子查询投影内 Prev(1,id) 按窗口历史求值，OM/Compile 变体与 EPL 运行时语义一致同测覆盖）、SameEvent（同流 wildcard 标量子查询返回触发事件本身，Compile/OM 变体同测覆盖）、SelectWildcard（异流 wildcard 返回最新内流事件负载）、SelectWithWhere2Subquery（外层 where 中两个标量子查询 OR 组合）、SceneOne（irstream + 相关子查询，length(2) 滑出时 old 行保留原始计算值）。新增 14 个 Java runtime 已登记到 case.epl-subselect-filtered 与 epl.subselect.filtered（status partial）。Joined2/3/4Streams 多流相关与数值 coercion、JoinFilteredOne/Two、SubselectMixMax（sort 窗口）、SubselectPrior（insert-into 链 + coalesce）、MultikeyWArray 数组相关键仍为后续切片；SelectWildcardNoName 的 subselect_1 自动命名为 EPL 语法糖（Go 要求显式别名，approved difference）。Java TestSuiteEPLSubselect 19/19 通过。Coverage 1450/4140 (35.02%). All gates pass.

本轮扩展 EPLOtherDistinct 切片至 16/20 execution：新增四个对照——OnSelectMultikeyWArray（on-trigger SelectFromNamedWindow + 数组 key distinct；修正 trigger.go 在 trigger select 结果路径补 distinctSnapshotResults 去重）、VariantStream（RegisterVariant + InsertInto 路由事件至 variant 流 + 三条 distinct 查询覆盖 wildcard/single-array/two-array）、Subquery（SubqueryIn + AsRecord 表达 IN 子查询中的 distinct，验证重复 id 不影响 IN 匹配语义）、OnDemandAndOnSelect（CreateNamedWindow + InsertNamedWindow 填充 + ExecuteFireAndForget distinct + order by 对照 Java FAF 矩阵）。新增 4 个 Java runtime 已登记到 case.epl-other-distinct 与 epl.other.distinct。BatchWindowJoin（length-batch join delta distinct）、JoinPatternOne/Two（every-distinct pattern + unidirectional join）仍为后续切片；EPLOtherBeanEventWildcardSODA 为 approved-difference。Coverage 1436/4140 (34.69%). All gates pass。

本轮开启 EPLOtherDistinct 切片（12/20 execution）：修正 distinct 核心语义——applyDistinct 对 new 结果改为「单次窗口更新内首次出现」去重（连续视图 keepall 每更新单事件故每事件重发；批处理视图 length_batch 整批一次更新故批内去重），同时全局引用计数跟踪每个 key 共享的事件数，old 流仅在最后一个共享事件离开时触发；snapshotBatch 与各 output 限速 flush 路径（OutputEvery 计数、OutputEveryTime 时间、snapshot）补 distinctSnapshotResults 去重；wildcard（select *）结果经 resultEvent 承载，snapshot helper 同时读 row/event 两种 Result。新增 epl_other_distinct_parity_test.go 十一个对照 + 一个 FAF 对照：OutputSimpleColumn（单流+join keepall 逐事件重发）、BatchWindow/BatchWindowInsertInto（length_batch 批内去重 + insert-into 跨 listener 调用累积，对应 Java getNewDataListFlattened）、BeanEventWildcardThisProperty/PlusCols/MapEventWildcard（wildcard distinct）、OutputLimitEveryColumn/RateSnapshotColumn（output 限速 flush 去重）、OutputLimitMultikeyWArray 单/双数组（OutputEveryTime 时间 flush + 数组 key encodeKey）、IterateMultikeyWArray（snapshot 数组 key）、FireAndForgetMultikeyWArray（FAF named-window 数组 key distinct）。distinctListener 改为累积式以贴合 Java getNewDataListFlattened 语义。新增 12 个 Java runtime 已登记到 case.epl-other-distinct 与 epl.other.distinct（status partial）。BatchWindowJoin（length-batch join delta distinct）、OnDemandAndOnSelect、Subquery、OnSelectMultikeyWArray、VariantStream、JoinPatternOne/Two（every-distinct pattern + unidirectional join）仍为后续切片；EPLOtherBeanEventWildcardSODA 为 approved-difference（Go 用不可变 fluent Plan 而非 SODA 文本/模型解析器）。


本轮关闭 EPLFromClauseMethodWConfig 五个类 17 个 execution：新增 fromclausemethod_cache_parity_test.go、fromclausemethod_multikey_parity_test.go、fromclausemethod_variable_parity_test.go 与 fromclausemethod_joinperf_parity_test.go 四个对照文件。CacheLRU/CacheExpiry 用 CachedMethodProvider（MethodCacheConfig LRUSize=3 / MaxAge=1s+PurgeInterval=10s）包装可计数 provider，逐事件断言 LRU 淘汰与过期命中调用次数；MultikeyWArray 以 Func2+slices.Equal 表达数组字段相等 where 谓词，并以数组内容/null 数组为缓存 key（encodeKey 的 %T:%#v 天然区分 []int 与 nil）；Variable 用 RegisterVariable/RegisterContextVariable 覆盖常量/非常量/context 分区变量服务，on-set 语句写 postfix，ContextVariable 以 CreateOverlappingPatternInitiatedTerminatedContext + ContextPatternField 按分区隔离 postfix，VariableMapAndOA 覆盖变量驱动 map/OA 方法 handler 的 iterator-only 快照；JoinPerformance 四个 correctness port（grid 与轮次缩减，Go 无 join on-condition index 故 cross-product 评估，timing 断言按既有 perf parity 惯例略）。为此重构 updateJoin 无依赖方法侧语义：方法行改为只 poll 新接受的 trigger 行并按 lineage 保留、随 trigger 行级联过期（removeStoredEventsCascade），取代原先每周期清空+全量重 poll，使调用次数与缓存命中贴合 Java 每事件一次 poll 的语义；同时区分混合语句（有事件驱动源）与纯方法语句（无事件驱动源）——前者在无 trigger 接受的周期（无关类型事件或被 filter 拒绝）不再 poll 方法侧，避免幽灵行；pattern-initiated overlapping context 的分区 key 改由 initiating 事件身份 + 语句内序号派生，使跨语句 context 变量共享一致。EPLFromClauseMethodVariableInvalid 保持 approved-difference：footprint/null-metadata 反射诊断在链式 API 无对应，context 变量作用域校验由 TestFromClauseMethodContextVariableParity 与 TestContextVariableScopeValidation 覆盖。新增 17 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethodWConfig 5/5、TestSuiteEPLFromClauseMethod 3/3 通过；unidirectional-historical、完整 Java 诊断文本与 join on-condition index 优化仍为后续切片；本轮不需要 MySQL Docker。

本轮关闭 EPLFromClauseMethod 主类全部 20 个 execution：新增 fromclausemethod_eventbean_parity_test.go 六个对照，覆盖 2JoinEventItselfProvidesMethod（事件类自身静态方法提供的 [10:20] 区间界——EvaluateOnce lower/upper 种子行驱动 join-where 过滤，9/21 静默、10/20 触发）、StreamNameWContext 与 WithMethodResultParam（provider 经 MethodInvocationContext 观察语句名，嵌套方法结果参数折叠进 provider，产出 somevalue/E1/s0 行）、EventBeanArray（Java EventBean[]/Collection/Iterator 三种返回形态统一为 Go []Event provider 契约，事件类型由 provider schema 声明，@type 注解及其限制诊断无 Go 对应，已登记差异）、InvocationTargetEx（provider 错误直接作为 SendEvent 错误返回，对应 Java 重抛的调用目标异常）与 UDFAndScriptReturningEvents（UDF/JS 脚本事件生产者统一折叠进 provider，JVM JS 引擎宿主保持 Java 专属并登记差异）。至此 case.epl-fromclausemethod-basic 由 partial 转为 mapped（主类 20 + NStream + OuterNStream 全部登记）；EPLFromClauseMethodCacheLRU/CacheExpiry/JoinPerformance/MultikeyWArray/Variable 五个类、context 绑定、unidirectional-historical、多事件流无依赖方法绑定与完整 Java 诊断仍为后续切片。新增 6 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethod 3/3 通过；本轮不需要 MySQL Docker。

本轮补齐方法源 with-arg 四切片：对照 Java EPLFromClauseMethod 主类的 ArrayWithArg/ObjectWithArg/2StreamMaxAggregation/NoJoinIterateVariables，新增 fromclausemethod_witharg_parity_test.go 四个对照。ArrayWithArg 以 fetchArrayGen(intPrimitive) 覆盖 method-first/declared 两文本顺序，逐事件断言 irstream——E3 只产生 {A,E3}，E6/E7 到达时过期事件携带各自轮询行离开（old {A,E3}、{A,E4},{B,E4}）；ObjectWithArg 用空串建模 Java null theString，fetchObject 单行/空结果对照（SODA 文本等价变体按惯例跳过并登记）。为此修正 join 运行时：事件驱动 join 中无依赖方法侧按接受触发的流的保留行逐行重求值（joinCycleOrder 让事件侧先于查找侧，updateJoin 内轮询行携带 trigger 行 lineage），轮询行只在所属 trigger 元组内成组并随其过期消失，贴合 Esper 每事件轮询由 join 窗口保留的语义；无触发单方法源语句的 Snapshot 现在用当前变量重轮询（currentStreamEvents），NoJoinIterateVariables 的 {5..10}/null/{4} 迭代器矩阵与 listener 全程静默由此固定；2StreamMaxAggregation 覆盖 unique(theString) × 100 行网格的 max(col1)={9}（重复键事件按 Java assertPropsPerRowLastNew 等价建模）。新增 4 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethod 3/3 通过；EventBeanArray、UDF/script、StreamNameWContext、WithMethodResultParam、InvocationTargetEx、context 绑定、multikey/array-key、cache、unidirectional-historical、多事件流无依赖方法绑定与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

## 2026-08-08

本轮补齐方法源 2JoinHistorical 五兄弟切片：对照 Java EPLFromClauseMethod 主类的 2JoinHistoricalIndependentOuter/SubordinateOuter/SubordinateOuterMultiField/OnlyDependent/OnlyIndependent，新增 fromclausemethod_historical_join_parity_test.go 五个对照。IndependentOuter 以两个 EvaluateOnce 常量方法源（fetchResult12/fetchResult23 值网格）覆盖 LEFT/RIGHT/FULL/FULL-reversed 四文本变体的部署即迭代器状态 {1,null},{2,2} 与 FULL 追加 {null,3}；SubordinateOuter 让 s1（fetchResult23(s0.value)）经 DependingOn("s0") 绑定，部署期种子从 EvaluateOnce 侧按拓扑序级联到无触发 subordinate 方法源（seedEvaluateOnceJoinSidesLocked 第二轮），依赖行携带 lineage 永不锚定占位元组，四变体同为 {1,null},{2,2}；SubordinateOuterMultiField 用 keep-all 事件流 LEFT OUTER JOIN 100 行常量网格的双字段等值条件对照 lastNew 与迭代器。OnlyDependent/OnlyIndependent 以 RegisterVariable(name, nil) 对应 Java null 初始化 int 变量，on-set 事件写入 lower/upper 后重轮询 fetchBetween/fetchBetweenString/fetchIdDelimited，并固定 Java 语义：无触发纯方法语句（queryIteratorOnlyMethodSources）只做状态刷新、永不投递 listener/route 输出（含 undeploy 后），SQL-historical 源保持 Go 原生任意事件轮询投递行为不变。新增 5 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethod 3/3 通过；ArrayWithArg/ObjectWithArg、2StreamMaxAggregation、NoJoinIterateVariables、EventBeanArray、UDF/script、context 绑定、multikey/array-key、cache、performance 与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

本轮补齐方法源 NW-twice 切片：对照 Java EPLFromClauseMethod3Stream1HistStreamNWTwice，新增 Stream.As(alias) 链式别名——同一个 keepall Named Window 在单条 Join 中以 us/them 两个逻辑名出现两次，方法源通过 DependingOn("us","them") 按别名做依赖拓扑排序与 invocation 笛卡尔展开，request.Dependency 按别名读取两侧窗口事件；窗口目录查找与事件匹配仍使用注册名（runtime 的 methodJoinEvaluationOrder 与 methodDependencyInvocations 统一走 logicalName）。Go 侧用 InsertNamedWindow 替代 Java 的 insert-into 语句填充窗口，SelectSourceEvent 投影 us/them 完整事件并对照 Java 的两行 {one,two,1}/{two,one,1} 结果。新增 1 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethod 3/3 通过；outer joins、cache、context、multikey/array-key、performance 与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

本轮补齐方法源 OuterNStream 切片：对照 Java EPLFromClauseMethodOuterNStream，新增 Stream.EvaluateOnce() 链式标记——常量参数方法源在 Deploy 时按 Esper statement-start 语义单次求值（seedEvaluateOnceJoinSidesLocked），像只读数据窗口一样保留且不发 listener 批次，触发周期不再清理或重查该侧；validateNode 拒绝 EvaluateOnce 与 trigger/DependingOn 组合。修正 outer join 占位语义贴合 Esper N-way 装配：依赖绑定（subordinate）方法行不再锚定占位元组（joinStoredTupleHasAnchor，joinChainedTuples/joinOuterTuples 的 outer 保留分支统一跳过），后续 outer 边无法求值时（累积元组中不含该边条件引用的真实事件）部分元组原样透传（joinChainedEdgeAnchored）。新增 fromclausemethod_outer_parity_test.go 五个对照：1Stream2HistStarSubordinateInner（s0-first/h0-first 两变体）、1Stream2HistStarSubordinateLeftRight（LEFT×2、h1-first RIGHT+LEFT、h0-first RIGHT+LEFT、FULL×2 四变体，逐事件断言 lastNew 与迭代器全量）、2Stream1HistStarSubordinateLeftRight（LEFT+RIGHT、LEFT×2、RIGHT×2 三变体，fetchVal(s0.id||'H0',s0.p00) 依赖读取）、1Stream2HistStarNoSubordinateLeftRight（EvaluateOnce 部署即 4 行占位、占位随匹配蒸发；Java 第二文本变体 h1 LEFT s0 RIGHT h0 在 Java 断言同一矩阵，按等价 s0 锚定 RIGHT×2 链建模并在 manifest difference 登记）、Invalid（own-outer-join-descendant 与 optional-dependency 两种无法满足的 outer 依赖诊断）。新增 4 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod；outer forward/3-hist subordinate 精确矩阵、cache、context、multikey/array-key、performance 与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

本轮关闭 OuterNStream 剩余三个精确矩阵：对照 tryAssertionThree/Four/Five 补齐 #lastevent 锚定的 outer forward subordinate——1Stream2HistForwardSubordinate（LEFT×2 链，fetchVal('H0',p00)/fetchVal('H1',p01) 常量前缀+依赖行计数 provider）、1Stream3HistForwardSubordinate（LEFT×3 链与 h0-first RIGHT+LEFT+FULL 混合变体均按声明建模，FULL 边上依赖绑定行不锚定占位、无法求值的边透传部分元组，两个变体共享同一事件矩阵）、1Stream3HistForwardSubordinateChain（fetchVal(s0.id||'-H0')/fetchVal(h0.val||'-H1')/fetchVal(h1.val||'-H2') 链式前缀，lastevent 下 trigger 即当前 s0 行）。三个 runtime（b2283f54/51307ad5/9cc6163d）此前已随 NStream 同名类登记，本轮补齐精确对照后 epl.fromclausemethod 的 OuterNStream 覆盖完整；cache、variable/context 绑定、multikey/array-key、performance 与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

## 历史切片记录（自 README 迁入，按原顺序）

本轮 Context Table 与子查询索引补充：相同 primary key 可在不同 Context partition 独立存在；live trigger、分区内 select/update/delete、Context FAF 与 aggregate `IntoTable` scoped replacement 均有对照测试，temporal/initiated partition 释放会回收 scoped Table state 与 ownership。普通全局 Table 的 Context FAF subquery 继续走 indexed candidate；Context-scoped Table 的相关子查询已支持当前 partition 的 primary/secondary hash 与 equality-prefix + B-tree candidate，普通 FAF/Join 会合并 root 与已物化 scoped state 的 candidate；root 中存在 legacy row 时保留 ownership-aware snapshot fallback。Named Window 的 equality/hash 与 equality-prefix + range B-tree subquery shared-index 子集已支持自动创建、显式使用、consumer disable/no-index、Context partition inheritance、multiple-index hint、结构化 index choice、multikey/array key、update/delete rebuild 和 live listener/late-start 对照，并补充 Build invalid 边界。跨 statement transaction、完整 routed/listener rollback、JVM query-plan hook、Esper 完整 cost/selectivity 与性能阈值仍待后续切片。

本轮结果集切片补充了索引化 `First`/`Last`/`Nth`、`WindowEvents`/`EventValue`、`MinByEver`/`MaxByEver`、带过滤器的有状态 `Leaving`、常量/过滤速率与 `RateByTimestamp`/数量速率、多条件 `SortedEvents`、外层分组可嵌套的 `LocalGroupBy`、`SortedAccessBy` 导航访问、无参数 `Method`/属性链、命名及环境注册式 `PluginAggregate`、按组隔离的 `AggregatePluginFactory`（含 `Enter`/`Leave`/`Value`/`Clear`）、context-partitioned rollup/FAF 聚合、确定性 `CountMinSketchAdd`、`TableSink` 聚合落表、链式 `AggregateStream.IntoTable` 原子快照同步、live table snapshot join，以及聚合投影别名 `ResultField` 的 order-by/having 语义；对应 Java 来源和 runtime ID 已登记在 `compat/capability-manifest.json`。这些能力仍是 mapped/partial 切片，不能解读为 Esper 聚合全量对等。

EsperIO 已开始落地 Go 垂直切片：`connectors/csv` 提供 CSV/File source、unformatted line source、CSV sink、Java 风格标题行/类型转换、loop/reset、固定速率/时间戳回放、Esper Event/Row bridge；`connectors/db` 提供 database/sql DML/Upsert sink、绑定、重试、prepared statement 生命周期、same-thread/命名固定 worker 异步 executor、Task 错误观察、优雅 drain 和 MySQL round-trip；`connectors/http` 提供 HTTP client/server source/sink、query/property URI 模板、JSON body、响应上限、重试、暂停/重启和 Engine bridge；`connectors/socket` 提供 TCP source、OBJECT/CSV/PROPERTY_ORDERED_CSV/JSON、Java escape、多连接、暂停/重启、typed Engine bridge 和可注册 object decoder；`connectors/kafka` 提供 kafka-go Reader/Writer、JSON processor、commit/retry/timestamp、key/header/有序 JSON 输出和 Engine bridge；`connectors/amqp` 提供 RabbitMQ Consumer/Publisher、queue/exchange/binding、prefetch、JSON/GOB codec、auto/manual ack、reject/requeue、retry 和 Engine bridge；`connectors/jms` 提供 provider-neutral Spring JMS bridge contract、Map/Text/Object/Bytes codec、Java event-type property、ack-after-process 和 channel transport；七者都使用统一 `OPENED/STARTED/PAUSED/DESTROYED` 生命周期。Java `esperio-csv` Maven 模块 80/80 通过，`esperio-db` 的 2/4 环境结果、`esperio-http` 的 3/4 结果、`esperio-socket` 的 7/7 结果，以及固定 Kafka 3.8.1 KRaft 环境下 `esperio-kafka` 的 6/6 结果已登记到 capability manifest；Spring JMS 4 个测试中 1/4 通过且环境差异已记录，Go Kafka/RabbitMQ/JMS bridge 也通过。Kafka 高级 group/rebalance/plugin、AMQP Java serialization/高级重连、JVM JMS provider/session/transaction/Spring XML 和完整 Dataflow connector 集成仍在计划中。

SQL FAF 的 `EPLDatabaseFAF` 10 个 Java execution 已有 `database_faf_parity_test.go` 对照用例，覆盖多行快照、column/whole-row hook、prepared reuse/close、typed substitution、distinct/where、变量、fluent Plan、SQL 文本参数子查询和 invalid/closed query；`SQLHistoricalProviderOptions.ParameterTypes` 声明 opaque SQL 参数类型，`RowConverter` 支持整行物化与 nil-row skip。fake `database/sql` driver 单元测试不替代上面的真实 MySQL 门禁。

本轮新增基础五字段及可选秒/毫秒 `CronSchedule`/`OutputAt` 与 Pattern `TimerCron`：支持 `CronEvery`、`CronRange`、`CronWildcard`、变量/参数字段、虚拟时钟下一次触发和日月/周 OR 语义；不等于 Esper 全部 cron 能力。

本轮继续补充 JSON provided-underlying 的 Go-native 泛型结构体适配：支持嵌套 slice/struct、JSON tag、UUID、`DateOnly`、`time.Time` 和 `big.Rat`，并通过 `Engine.SendJSON`/`RenderJSON` 验证解析、发送和渲染边界；同时覆盖 schema invalid、constructor-style primitive default、null projection route 和 pattern typed-array。JVM 专属的 public-class/default-constructor/non-static-inner-class 诊断记录为 Go 零值结构体模型的 API 差异。

本轮继续补齐 JSON VM-class/laxness 对照：新增 Go-native `URL`/`URI`，覆盖 UUID、时间、URL、URI 的 scalar/一维数组/二维数组/collection、null、shape mismatch 和渲染；覆盖 byte/short/int/long/float/double/BigInteger/BigDecimal 对应 Go 类型的字符串宽松转换及非法值诊断，并验证 malformed/trailing JSON 与 static/dynamic undeclared-field policy。typed 特殊标量的非法输入现在返回错误，不再静默变成 Null；随后又补齐 core dynamic JSON 的 string/number/bool/null/object/array、混合/嵌套数组和 `42.0`/指数数字词法 round-trip。Java class/list adapter 的其它约定、递归 schema/metadata、完整 nested/dynamic arbitrary-precision 格式和共享 parse-write trace 仍未完成。

本轮继续对照 `EventJsonTypingClassParseWrite`：`NewJSONSchemaFor[T]`/`ParseJSON`/`RenderJSON` 现在覆盖 class simple、递归 nested class、对象的一维/二维数组与 collection、基础类型/Character/rune/BigInteger/BigDecimal 列表、枚举样字符串列表，以及 UUID/LocalDate/LocalDateTime/OffsetDateTime/ZonedDateTime/URL/URI 的 scalar/数组/collection。新增用例固定 missing、null、empty、partial、schema 字段顺序、Character 字符串形态和 typed number lexeme；这组 JSON 对照不需要 MySQL Docker。

本轮补充 Java `InfraNWTableFAF` 对照：`infra_nwtable_faf_parity_test.go` 覆盖 Named Window/Table 的 wildcard、filter、distinct、count/sum、group、IN、二流/三流 join 与 join where；`faf_mutation_test.go` 用 Go-native `OnDemand()` 链覆盖 insert/update/delete/delete-all、参数绑定、`InitialNamedWindowField`、Named Window consumer delta，以及 `WithContext` 下 hash/category selector 的分区内 delete/update/delete-all。Named Window 保存稳定的 partition properties，避免更新后的事件字段改变 selector 所属分区。另修正 ungrouped row-for-event 聚合，使每个源事件都返回共享 aggregate 值。Java `TestSuiteInfraNWTable` 26/26 通过。

本轮继续复核 Java `InfraUpdate`：新增 `OnDemand().UpdateAll(...)`，覆盖 Named Window/Table 无 `where` 全量更新、ordered assignment、`InitialNamedWindowField`/`InitialTableField` 和两种存储的 FAF 结果契约；`Table.Update` 允许主键变更并原位 re-key，保持插入顺序，同步 hash/unique/B-tree index，主键或唯一 secondary index 冲突时保持原子不变。新增 `TestOnDemandUpdateAllMatchesInfraUpdate` 与 `TestTableUpdateRekeysPrimaryKeyInPlaceAndPreservesIndexes`。该切片不需要 MySQL Docker。

本轮继续关闭 Context Table 的持久行归属遗漏：Table 行保留稳定内部 identity，Engine 按 table/context/row identity 记录首次 context partition、代表事件和 selector properties；Context FAF 更新分区字段或主键后，后续 update/delete/delete-all 仍按原 partition 执行，真正删除和 `DestroyContext` 清理归属。强化 `TestOnDemandContextPartitionUpdateAndDeleteAllRespectSelector`，覆盖 Table/Named Window、category selector、分区字段变化、primary-key re-key、原分区删除和剩余分区清理。FAF 目标存储 mutation 在中途主键/唯一冲突或 pending routed processing 失败时会回滚 Table/Named Window 目标状态及 Context ownership；完整跨 statement transaction、跨目标 routed side effect、更广 live Context Table mutation 组合和 transaction/concurrency trace 仍未完成；本轮不需要 MySQL Docker。

本轮补齐 Java `ContextKeySegmentedInfra.ContextKeyedSegmentedTable` 的 live Table ownership：`TestContextTableLiveInsertOwnershipMatchesEsper` 用 Go 链式 `InsertIntoTable`、`UpsertIntoTable`、`MergeInsertIntoTable` 覆盖 A/B 分区写入，更新分区字段后仍由原分区 selector 执行 FAF 删除，并验证 live delete 清理 row identity ownership。Java `TestSuiteContext` 17/17 通过；跨 Context 相同主键隔离、aggregate-into-table 与完整 Context Table transaction/trace 仍待后续切片，本轮不需要 MySQL Docker。

本轮新增 `faf_multirow_test.go` 对照 Java `InfraNWTableFAFInsertMultirow`：`OnDemand().InsertRows(InsertValues(...))` 支持 Named Window/Table positional multi-row insert、Named Window 子查询值、1000 行上限、列数诊断和 unique/primary-key 冲突整批回滚；`NamedWindowUniqueIndex` 提供严格唯一约束并区别于 `Unique` retention 的替换语义。Go immutable fluent Plan 对应 Java SODA/model 路径，核心验证不需要启动 MySQL Docker。

本轮新增 `faf_substitution_params_test.go` 对照 Java `InfraNWTableFAFSubstitutionParams` 的 7 个 execution：增加 1-based typed `ParameterAt[T]`/`PositionalParameter[T]` 与 `ExecuteFireAndForgetWithPositionalParameters`、`PreparedQuery.ExecuteWithPositionalParameters`，覆盖 Named Window/Table 的单值、双值、slice-IN、具名参数、重复参数、缺参、额外参数、混用、跳号和类型错误。Go API 保留具名 `Parameter[T]` 的 immutable map 绑定；EPL 保留关键字解析和精确 Java 诊断不属于 Go fluent 契约。该切片不需要 MySQL Docker；FAF 跨 join/subquery/context 的参数组合、index plan、representation、历史/方法源、事务/并发和完整 trace 仍未完成，module resolve 已在后续切片补齐。

本轮继续新增 `faf_subquery_parity_test.go` 对照 Java `InfraNWTableFAFSubquery`：用 Go-native `SubqueryValue`/`SubqueryGroupRows`、`OuterField` 和 `OnDemand()` 覆盖 Named Window/Table scalar、join、insert、未相关及相关 update/delete/select、Context 两窗口/全局窗口、inner where/group-by，并在 FAF 执行边界拒绝 event-stream source、source filter 和 context mismatch。目标行通过显式 `OuterEvent` 传入相关子查询；新增 `faf_subquery_index_parity_test.go`，让 Context FAF 中基于 `OuterField`/参数的完整 equality/IN scalar/exists 子查询对 Named Window/Table 声明索引执行 candidate lookup，覆盖全量/指定 partition selector、Context-scoped Named Window/Table 的 partition-local index lookup、equality-prefix + B-tree range、root legacy row 的 ownership-aware snapshot fallback、index-hit counter，以及 UDF/Null 等不安全形状的快照回退。Java 的 4 个性能/index execution 仍不宣称 timing parity；本轮仍不需要 MySQL Docker。

本轮新增 `faf_resolve_parity_test.go` 对照 Java `InfraNWTableFAFResolve`：`Environment.RegisterModule`/`Module.RegisterNamedWindow`/`Module.RegisterTable` 与 `Module.NamedWindow`/`Module.Table` 提供 Go-native module namespace，覆盖 A/B module 中同名 `MyInfra` 的 Named Window/Table 独立写入、wildcard FAF snapshot、module-bound runtime lookup、Plan hash/canonical 隔离和 unknown-module Build 失败。Go 不解析 EPL `module` 文本，也不复制 Java `ModuleUsesOption` 或 protected/public class-loader visibility；该差异已登记到 `case.query-fire-and-forget-resolve`。本轮不需要 MySQL Docker。

本轮新增 `faf_index_parity_test.go` 对照 Java `InfraNWTableFAFIndex` 的 12 个 runtime：`IndexHash`/`IndexBTree`、`SecondaryIndex`/`UniqueIndex`、`NamedWindowIndex`/`NamedWindowBTreeIndex`、`UseIndex`/`UseIndexOn` 和 `Plan.IndexPlan()` 覆盖单源/Join 的自动选择、IN/equality/range、数组/复合数组/null key、错误 hint 与聚合 where；`NamedWindow.Lookup` 及生命周期测试验证声明索引在更新、删除、淘汰和回滚恢复后不残留。新增物理执行对照后，Table/Named Window 的单源完整 equality/IN（含 prepared named parameter）会真正走 hash candidate lookup；普通内连接及两流 `LeftOuter`/`RightOuter` 的 optional side 也会按完整 equality key 和 B-tree range 执行 Table/Named Window candidate lookup，支持自动/显式 index、复合 equality-prefix、双边 inclusive range、反向操作数、`JoinWhere`、批量 probe 去重、source filter、preserved-side unmatched 行和插入顺序；Context 单源 FAF 复用同一候选路径，Context-scoped Table 会合并 root 与已物化 scoped index state，新增 Context inner Join、两流 `LeftOuter`/`RightOuter` optional-side，以及相邻条件左深 mixed `Inner`/`LeftOuter` chain 的 equality/range candidate，覆盖 Named Window/Table、全量/指定 partition selector 和索引命中计数，并按 context key 二次过滤索引结果；`faf_subquery_index_parity_test.go` 继续覆盖 Context FAF 子查询的 correlated `OuterField`/parameter equality/IN scalar/exists，以及 equality-prefix + B-tree range candidate，覆盖全量/指定 partition selector、Context-scoped Named Window/Table 的 partition-local index lookup、参数化上下界、root legacy fallback 和 UDF fallback。此外，`TestInfraNWTableSubqCorrelIndexSharingParity`/`TestInfraNWTableSubqCorrelIndexSharingContextPartitionParity`/`TestInfraNWTableSubqCorrelIndexSharingBTreeParity`/`TestInfraNWTableSubqCorrelIndexOptionValidationParity`/`TestInfraNWTableSubqCorrelIndexMultipleIndexHintsParity`/`TestInfraNWTableSubqCorrelIndexChoiceParity`/`TestInfraNWTableSubqIndexShareMultikeyArrayParity` 对照 Java `InfraNWTableSubqCorrelIndex` 的 equality/hash 与 equality-prefix + range B-tree shared-index 子集，覆盖自动 shared index、显式 index、consumer disable/no-index、Context inheritance、插入顺序、multiple-index hint、结构化 index choice、显式/自动 multikey-array key、update/delete rebuild 和 Build invalid。`FullOuter`、右保留或非相邻链式 outer、unidirectional、OR、UDF、Null/Missing、复杂动态表达式和超大多流 probe 安全回退。单源 B-tree range 继续覆盖反向 Between、prepared bounds、Null 边界回退以及更新/删除/清空后的索引维护。Go 不复制 EPL `@Hint` 与 JVM backing-class hook，Context right/full-preserving/non-adjacent chain/unidirectional Join、Esper 完整 cost/selectivity、JVM query-plan hook 和性能阈值继续保持 partial。本轮不需要 MySQL Docker。

本轮又补充 Context FAF Join 的相邻条件左深 mixed chain candidate：仅对 `LeftOuter`/`Inner` 边、且每条边只引用紧邻前一源和新源的链启用 equality/hash 或 equality-prefix + B-tree range 候选；`TestInfraFAFContextChainedMixedOuterIndexCandidateParity` 覆盖 Named Window/Table、`LeftOuter -> Inner`/`LeftOuter -> LeftOuter`、全量/指定 partition、缺失中间源、稳定顺序和 middle/tail 命中计数。RightOuter/FullOuter、非相邻链、unidirectional 与无法安全提取的条件仍回退 snapshot；本轮不需要 MySQL Docker。

本轮补充真正的 B-tree cursor：`lookupRangeMany` 先按 equality prefix 与 lower/upper boundary 做二分区间定位，再在候选区间内保留最终 `indexRangeEntryMatches` 校验；不可比较的 Null/Missing/异构值自动回退完整有序扫描。`TestIndexRangeCursorBoundsAndFallback` 与既有 Table/Named Window range、复合 prefix、更新删除和 Context Join 对照共同固定结果与回退语义；本轮不需要 MySQL Docker。

本轮继续补齐 `EventJsonTypingCoreParse/CoreWrite`：`json_core_parity_test.go` 直接覆盖基本 scalar、Character 多字符取首 code point、wrapper/primitive 一维与二维数组、enum、`big.Int`/`big.Rat`、`Object`/`Object[]`/`Map` 容器，以及嵌套 BigDecimal 对象和数组的解析/渲染矩阵；`RenderJSON` 对 Character 恢复单字符 JSON string。Core JSON 定向测试通过，Java `TestSuiteEventJson` 13/13 仍通过；递归 schema/metadata、完整 dynamic strict-lax、所有 nested/dynamic arbitrary-precision 格式规则和共享 Java/Go trace 仍保持 partial。本轮不需要 MySQL Docker。

本轮再次复核 JSON 遗漏入口：新增 `Engine.JSONSender`/`JSONEventSender` 的解析、发送和路由链、同名异构 schema 的 identity 防串型、`Schema.Getter` 的 schema-bound mapped getter、特殊字段名与 JSON/Map nested-schema 校验，以及显式泛型 `NewJSONFieldAdapter`/`NewNamedJSONFieldAdapter`/`WithJSONFieldAdapter` 的 string↔typed 双向转换、null 保留、类型/输入错误诊断和 Plan identity；同时覆盖 DocSamples 的 Book/Cake nested、dynamic empty、application class 和显式 Environment catalog boundary。sender dispatch 保留 JSON 原始字面量，Java `EventSenderJson`/`EventPropertyGetter`/annotation class-loader/module visibility 则以 Go 显式对象、函数注册和 catalog 隔离替代；完整 fragment/metadata 与 Java module/path 语义仍未完成。本轮不需要 MySQL Docker。

本轮继续补齐 typed JSON 的嵌套 fragment 元数据：struct-valued scalar/indexed 字段会自动生成稳定的 `NestedSchema`，递归指针复用有限 schema 图；新增 `NestedSchemaNames`/`FragmentSchema`、深层 schema-bound getter、`Event.GetFragment`/`GetFragments` 和 null-versus-empty nested-array 渲染对照，显式 `WithNestedPropertySchema` 仍优先。新增 `WithNestedPropertySchemaFrom`，可从显式 `Environment` catalog 解析独立注册的命名 JSON/Map fragment，并覆盖 Java `Friend[]`/`User[]` create-schema 形态、缺失/跨环境诊断和 JSON→Map fragment。该切片对照 Java `EventJsonTypingNestedRecursive`、`EventJsonTypingClassWArrayAndColl` 和 `EventJsonProvidedClass*WithCreateSchema` 入口，但完整 catalog 生命周期、全表示 fragment metadata、模块可见性和共享 parse-write trace 仍保持未完成。

本轮新增 `enum_data_sources_parity_test.go`：用 Go 链式 API 对照 `ExprEnumDataSources` 的 Prev/Sorted View、Named Window/Subquery、部署参数/变量 late binding、nested property schema、method-backed enum object、mapped property、collection cast、sorted access、generic component、access aggregate 和 Table source，新增 15 个 Java runtime 对账；join/pattern、match-recognize/context property、Java Optional 精确 generic/serde 与完整 invalid/type metadata 仍未完成。

示例位于 `examples/stage1`。当前仍是增量移植，不能宣称完整 Esper 对等：枚举/集合方法的完整 Java 类型与无效规则校验、BigDecimal/任意集合类型、复杂嵌套/子查询/访问聚合组合（当前仅有 Named Window/Table 基础子查询）、ROLLUP/CUBE/GROUPING SETS 的组合 grouping specification/Join/Context/FAF/output 全矩阵，Variant 的单列 method-backed member conversion、Java application/type-class metadata、全量动态属性/表示转换/FAF/Dataflow 矩阵，Join/Aggregate/Pattern/Named Window 路由的完整混合 new/old 顺序、事件表示/转换和可配置循环策略，完整多流/子查询 Join，历史 SQL 的方言占位符、metadata-origin/类型映射、连接池/事务/连接生命周期、DML/Upsert、方法流和多源 FAF/Context 约束，完整聚合和输出策略（filtered/math-context/访问聚合、cron 微秒精度、特殊日历运算符、完整 when/then 上下文与更多计数函数、完整 snapshot-after/grouped delivery 等；日历 after 目前只覆盖非负 years/months/days 的固定周期）、Pattern guard/observer timer-schedule/consumption 及 Match Recognize 的复杂 NFA/prev/interval/after/聚合/窗口删除语义、Context selector/嵌套/全生命周期、Dataflow 类型化多端口/信号/背压、完整事件格式与 Serde、EsperIO 其余传输连接器及完整 Dataflow/trace parity、Java/Go 全量行为差分和源测试映射仍按规划继续实现。Java 运行态 execution 清单已生成在 `compat/java-execution-inventory.jsonl`，但它不等于 Go 已通过对照。

本轮继续补齐方法源 N-stream 域：对照 Java EPLFromClauseMethodNStream，新增 FromAs[T] 支持同一事件类型在单条 Join 中以 s0/s1/s2 不同别名多次出现，并对照完成 2-stream + 2 hist 的 star-subordinate、3-stream + 1 hist 的 subordinate 切片。方法源通过 DependingOn("s0") 从 request.Dependencies 读取驱动流事件，fcmMakeSenderMatchFields 按 ids0/ids1/ids2 过滤 keepall 全量重放行，保留 Java assertPropsPerRowLastNew 语义。plan.go 的 validateNode、sourceSchema 对显式 isAlias 节点做条件式 schema 回退，避免无条件别名回退破坏既有 regression。新增 2 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethod 3/3 通过；NW-twice、pure-no-subordinate、outer joins、cache、variable/context、multikey/array-key、performance 与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

本轮继续补齐方法源变量驱动 3-historical 切片：对照 Java EPLFromClauseMethodNStream 的 3HistPureNoSubordinate、3Hist1Subordinate、3Hist2SubordinateChain，新增 FromMethod + RegisterVariable + OnEvent SetVariables 的 Go 链式对照测试；方法源从 request.Variables 读取 var1/var2/var3 行数，依赖方法源从 request.Dependencies 读取前一方法事件；Statement.Snapshot 对照 Java assertPropsPerRowIteratorAnyOrder。新增 3 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethod 3/3 通过；NW-twice、outer joins、cache、context、multikey/array-key、performance 与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

本轮继续对照 Java `InfraUpdate`/`InfraNWTableFAF` 的失败路径：`ExecuteFireAndForget` 在 Table 或 Named Window mutation 中途失败，以及 pending routed event processing 返回错误时，恢复目标存储、Context 分区状态、Context Table row ownership 和待派发队列；新增 `TestOnDemandTableMutationRollsBackAfterMidBatchFailure`、`TestOnDemandNamedWindowMutationRollsBackAfterAssignmentFailure`、`TestOnDemandMutationRollsBackAfterRoutedProcessingFailure` 与 `TestOnDemandContextTableMutationRollsBackAcrossPartitions`，覆盖 Table/Named Window 的跨行赋值失败、route cycle limit 失败及跨 category partition 的 primary-key collision。该回滚边界只保证当前 FAF 目标 mutation 的状态恢复，不等同于跨 statement、跨目标路由副作用、listener dispatch 或外部资源的完整事务；本轮不需要 MySQL Docker。
