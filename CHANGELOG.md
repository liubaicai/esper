# 迁移进度 Changelog

按切片记录 Esper 9.0.0 → Go 的迁移明细；[README](README.md) 只保留移植概况与大概进度。每完成一个切片在此追加记录，并同步 `compat/capability-manifest.json` 与 [实施规划](docs/esper-go-port-implementation-plan.md)。历史条目由 README 迁入，保持原始行文与顺序。

## 2026-08-09

本轮关闭 `ClientCompileVisibility` 12/12 execution。模块可见性从仅有 public/protected lifecycle 扩展为默认 `private`、显式 `PrivateModule()`/`ProtectedModule()`/`PublicModule()`；新增 immutable `ModulePath`，通过 `module.Uses(...)`/`env.Uses(...)` 以 Flink-style fluent API 表达 Java `CompilerPath` 与 `uses`，不解析 EPL。统一 resolver 覆盖 event type、variable、context、Named Window、Table、declared expression 和 registered Go script：同 module 可见 private/protected/public，外部仅看见 public；两个 public module 导出同名定义或 preconfigured catalog 与 module path 重名时返回结构化 `ErrorAmbiguous`，显式 `uses b` 对全部类别稳定选择 b，并进入 Plan canonical/hash identity。`Module.Build` 作为最终 module-name binding 覆盖规则已有 identity。Schema 新增 `BusEventType()` metadata 与 `Engine.SendBusRecord` 外部入口，只有 public module 中显式 bus-visible 的 event type 可进入；private/protected bus schema 与冲突 visibility options 在注册前失败。跨部署 public Named Window 固定 Java E1/E2/E3/E4、length(2) sum=10/30/45/51 轨迹。JVM application-inlined class 迁移为 module-local declared expression/typed Go UDF，不复制 Java source compiler、classloader、binary path 或精确 diagnostic 文本。新增 `client.compile.module-visibility` capability、mapped case 与 12 个 Java runtime。Coverage 1673/4140 (40.41%)，222 case 中 216 mapped / 0 partial / 6 approved-difference。当前有 JDK 17 但无 Maven，未执行 Java regression suite；本轮不需要 MySQL Docker。

本轮关闭 `ClientMultitenancyProtected` 6/6 execution：Named Window/Table、variable、context、event type 与 declared expression 均可在同一 Engine 内以相同逻辑名并行存在。新增 `ProtectedModule()`、`Module.Build`、module-qualified schema/variable/context/expression helper 与 immutable `Query.Module` identity；Table/Named Window trigger 通过 `TriggerQuery.InModule` 显式绑定目标，不解析 EPL/module text。Engine 在部署事务内原子 materialize protected module 的 Table、Named Window、variable 与 context runtime state，同一 protected module 重复活动部署返回结构化 deployment error；undeploy 释放资源与 Context partition/variable state，另一 module 继续接收事件，并允许原 immutable compile artifact 后续重新部署。Java 固定轨迹均已覆盖：A/B infra 各保留 E1/E2、10/20 variable 在 10s 后为 11/21 且撤销 A 后 B 在 20s 为 22、A/B initiated context 计数为 2/3 后 B 为 4、同名 `MySchema` 分别得到 count 0→1→2 与 sum null→10→30→60、同名 expression 分别为 1/2。同步修复 module-bound trigger result-schema/context validation，并使未分组 aggregate iterator 在尚无事件时返回 Java-compatible count=0/sum=null 行；全量门禁还暴露并修复 Dataflow `LogSink` 输入被误收窄为 Event/Row 的问题，primitive Beacon 现在按其 `any` 契约合法连接。Environment 保留可复用的 immutable compile-time module artifacts，Engine runtime 定义在 undeploy 后不可见；public/protected 跨 deployment uses/import 依赖图、ambiguity diagnostics 与其余 representation convenience 仍属后续。新增 `client.multitenancy.protected-deployment-namespace` capability、mapped case 与 6 个 Java runtime。Coverage 1661/4140 (40.12%)，221 case 中 215 mapped / 0 partial / 6 approved-difference。当前有 JDK 17 但无 Maven，未执行 Java regression suite；本轮不需要 MySQL Docker。

本轮关闭 `ClientInstrumentAuditDocSample` 与 `ClientInstrumentAuditAudit` 2/2 execution。新增 typed `AuditCategory` 14 类、`StatementAudit(...)` 与 `DataflowBuilder.Audit(...)`；空参数等价 Java bare `@Audit`，审计选择进入 statement metadata 与 Plan canonical identity。`AuditRecord` 固定 runtime URI、deployment ID、statement name、agent/context partition ID、category、虚拟时间和 message，`SubscribeAudit` 提供 Engine-local 可移除订阅；statement/Named Window/timer/context 路径在事务锁内排队并于解锁后投递，listener 可安全重入 Engine API，不复制 Java `AuditPath` 的 JVM-wide static callback/logger。STREAM 严格对照在 runtimeTime=1 交付 `SupportBean(theString=...) inserted SupportBean[SupportBean(E1, 1)]`；PROPERTY、EXPRESSION、EXPRESSION-NESTED、EXPRDEF、VIEW、PATTERN、PATTERN-INSTANCES、SCHEDULE、INSERT、CONTEXTPARTITION 与 DATAFLOW-SOURCE/OP/TRANSITION 均由真实 fluent runtime 路径覆盖。`AuditFormatter` 支持 `%u/%d/%s/%i/%c/%m/%tutc/%tzone`；Dataflow 因 Go definition 属于 Environment，使用 `deploymentId=environment`。新增 `client.instrument.audit` capability、mapped case 与 2 个 Java runtime。Coverage 1655/4140 (39.98%)，220 case 中 214 mapped / 0 partial / 6 approved-difference。当前有 JDK 17 但无 Maven，未执行 Java regression suite；本轮不需要 MySQL Docker。

本轮关闭 `ClientInstrumentMetricsReportingStmtMetrics`、`StmtGroups`、`DisableStatement`、`DisableRuntime` 与 `NW` 共 5/5 execution，由此完成 MetricsReporting 6/6 runtime。新增 `WithStatementMetrics`、`StatementMetricsGroup` 及有序 LIKE/regexp include/exclude、default include、inactive report；首个 `AdvanceTime` 建立各组独立 epoch，固定 6s/7s/8s/10s 周期、每次 report 后清零，并支持 `SetStatementMetricGroupInterval` 动态停启。`StatementMetric` 记录 runtime/deployment/statement/timestamp、CPU/wall、input 与 I/R stream output；`SubscribeStatementMetrics` 提供可移除 typed management listener，`CurrentStatementMetricGroups` 按 `group-default/group-N` 返回未 flush snapshot，`SetStatementMetricsEnabled` 与全局 `SetMetricsReportingEnabled` 分别复现单 statement 计数开关和 runtime+statement schedule pause/resume。部署自动注册、undeploy 移除；普通 filter、Named Window delta、timer/output observer 已接入计数。Named Window 环境声明作为 `deploymentId=environment` 的 metric entry，A/W 输入固定 2/1，B1/B2/C/D/M 固定 1/1/2/2/1。Linux 用进程 CPU clock，避免 goroutine 跨 OS thread 导致伪精度；非 Linux 无可移植 sampler 时为 0，wall/output/input 契约不变。Go 不把 metric 注入普通 event catalog，因此 management listener 不会像 Java metric statement 一样递归报告自身。`WithRuntimeMetrics(-1)` 现在可配置只读 snapshot 而不调度周期报告。新增 `client.instrument.statement-metrics` capability、mapped case 与 5 个 Java runtime。Coverage 1653/4140 (39.93%)，219 case 中 213 mapped / 0 partial / 6 approved-difference。当前有 JDK 17 但无 Maven，未执行 Java regression suite；本轮不需要 MySQL Docker。

本轮关闭 `ClientInstrumentMetricsReportingRuntimeMetrics` 1/1 execution。新增 `metrics.go`、`WithRuntimeMetrics(interval)`、immutable `RuntimeMetric`、`SubscribeRuntimeMetrics`/removable subscription、`CurrentRuntimeMetric` 与 `SetRuntimeMetricsEnabled`。首个显式 `AdvanceTime` 建立 reporting epoch，10 秒周期在 11s/21s 产生记录；输入累计 1→4、delta 1→3，严格复现 Java `MetricExecEngine` 先 capture count、再把 metric internal event 计入下一周期的顺序。一个 15.999s one-shot timer 在两次 report 之间完成，schedule depth 固定 1→0；当前 depth 统计“有 pending callback 的 active statement”，对本 execution 精确，但多 timer statement 的逐 handle 总数仍待后续。Go 以 typed management listener 交付，不把 `RuntimeMetric` 注册成普通 event stream；JVM `ThreadMXBean` per-thread CPU availability 是平台差异，不复制。statement metrics/groups、Named Window accounting 和 per-statement enable/disable 仍是后续 instrument 切片。全量 race 同时捕获并修复 `AsyncTask.Wait` 早于线程池 `Pending/Completed` 统计发布的顺序窗口：现在 stats、idle 与 admission capacity 先更新，task done 最后关闭，使 Wait 成为稳定观测屏障。新增 `client.instrument.runtime-metrics` capability、mapped case 与 1 个 Java runtime。Coverage 1648/4140 (39.81%)，218 case 中 212 mapped / 0 partial / 6 approved-difference。当前有 JDK 17 但无 Maven，未执行 Java regression suite；本轮不需要 MySQL Docker。

本轮处置 `ClientInstrumentInstrumentation` 1/1 execution。复核固定 Esper 9.0.0 源码发现 `InstrumentationHelper.ENABLED=false`、`ASSERTIONENABLED=false`，RegressionRunner 不会调用 start/end test，所有 internal instrumentation 分支为编译期 false；该 execution 仅 compile/deploy `select 1=2 from SupportBean` 后 undeploy，无事件、callback 或 trace 断言。因此单列为 N 级 approved difference：Go 测试以 typed `Equal(Literal(1), Literal(2))` Plan 固定 build、空 snapshot、undeploy/destroyed 生命周期，不复制 JVM-wide static helper、数百个 q/a callback、bytecode hook 与 system-property provider。`ClientInstrumentAudit` 和 6 个 MetricsReporting execution 仍是独立、未完成的可观察产品能力，不能被本 case 代替。新增 1 个 Java runtime disposition。Coverage 1647/4140 (39.78%)，217 case 中 211 mapped / 0 partial / 6 approved-difference。当前有 JDK 17 但无 Maven，未执行 Java regression suite；本轮不需要 MySQL Docker。

本轮关闭 `ClientMultitenancyInsertInto` 2/2 与 `ClientMultitenancyIndex.ClientMultitenancyIndexTable` 1/1 execution。单 module 的 producer/filtered consumer 由有序 `DeployPlans` 表达，固定 E1/E4 命中、E2/E3 过滤；两 module 场景用两个 immutable Plan 的独立 Deploy 表达 compiler path，producer 先运行时 E1 不被晚部署 consumer 回放，E2-E4 按 a/b projection 交付。Table 场景以 typed `CreateTable` 声明 composite primary key 和 `BasketIndex(symbol)`，插入 A/B 两行后由 unidirectional `VWAPrice` 事件 Join 得到 weight=1/2，并检查 Plan 显式选择 equality `BasketIndex`。Go 预注册 derived-stream schema，不复制 EPL module text、Java compiler-path access-modifier callback 或 JVM query-plan hook；当前 continuous Table Join 在部署时预装载 passive Table snapshot，因此本 case 证明计划选择和结果，不声称逐事件 physical index lookup。`ClientMultitenancyProtected` 的 deployment-local infra/variable/context/event type/expression namespace 仍留在独立后续切片。新增 `client.multitenancy.module-routing-index` capability、mapped case 与 3 个 Java runtime。Coverage 1646/4140 (39.76%)，216 case 中 211 mapped / 0 partial / 5 approved-difference。当前有 JDK 17 但无 Maven，未执行 Java regression suite；本轮不需要 MySQL Docker。

本轮完整关闭 `ClientRuntimeThreadedConfigInbound`、`ClientRuntimeThreadedConfigInboundFastShutdown`、`ClientRuntimeThreadedConfigOutbound`、`ClientRuntimeThreadedConfigRoute` 与 `ClientRuntimeThreadedConfigTimer` 各 1/1 execution，并由此完成 `suite/client/runtime` 52/52 runtime 的 manifest 处置。新增显式 Go-style Engine options `WithInboundWorkers`、`WithOutboundWorkers`、`WithRouteWorkers`、`WithTimerWorkers`；同步 `Send`/`Route`/`AdvanceTime` 契约保持不变，启用后使用 bounded worker pool、waitable/reusable `AsyncTask`、transitive `Engine.WaitAsync`、`ThreadingStats` 与 `SetThreadingErrorHandler`。Inbound 覆盖 map、struct、XML、ObjectArray、JSON 及异步错误；Outbound 把一个 statement 的 subscriber/listener/sink batch 整体排队，阻塞 observer 不再阻塞 send；Route/Timer 把完整 runtime cycle 移出调用方。Engine transaction mutex 仍串行化规则状态，不复制 Java 在单一事件周期内并发修改 100 个 statement state 的内部实现；显式 `AdvanceTimeAsync` 取代可选 wall-clock internal timer。关闭时先停止准入、取消并 drain queued tasks、解析全部 task handle、等待 worker 退出，再 undeploy；统计固定 accepted/completed/dropped/pending，取消的 waiter 不消费 task result。测试使用 blocking gate 和精确 delivery count 取代机器相关 `<100ms` 阈值与 executor 实现类检查。新增 `client.runtime.threading` capability、mapped case 与 5 个 Java runtime。Coverage 1643/4140 (39.69%)，215 case 中 210 mapped / 0 partial / 5 approved-difference。当前有 JDK 17 但无 Maven，未执行 Java regression suite；Java execution 以 inventory 与源测试逐项对照。本轮不需要 MySQL Docker。

本轮完整关闭 `ClientRuntimeSubscriber` 11/11 execution 与 `ClientRuntimeSubscriberDisallowed` 1/1 execution。新增 Go-style 单 subscriber 表面：`Statement.SetSubscriber`/`HasSubscriber`、`Subscriber`、immutable `SubscriberUpdate`/`SubscriberRow`、`Engine.SetSubscriberErrorHandler` 与 fluent `DisallowSubscriber()`。subscriber 与 listener/sink 共用同一 statement dispatch batch，但保持一个可替换 observer：nil 移除，undeploy 清空，redeploy 需重新绑定，listener 可独立增删；`SubscriberRow` 显式提供 ordered `Values`、`Map`、`Get` 和 wildcard `Underlying`，join source Event 值转换为具体 Go underlying，避免复制 Java `update`/named method/static method/overload/vararg/updateRStream 的反射方法匹配。subscriber 返回 error 或 panic 会被隔离并通过 engine handler 报告，不会让 Send/Route 失败，也不会阻断 listener/sink；Go Listener 继续保留既有 fail-fast error contract。`DisallowSubscriber` 进入 Plan canonical identity，安装时返回结构化 `ErrorInvalidRule`，listener 仍可用。为覆盖 Java variable subscriber，on-set trigger 现在按变量声明顺序产生一个 assignment Row；Named Window insert/delete/select subscriber 分别沿统一 ResultBatch 的 new/old stream 观察 mutation。`client_runtime_subscriber_parity_test.go` 覆盖 wildcard/map/ordered values/null、length-batch IR stream、subscriber+listener、join、error/panic isolation、Named Window、variable、undeploy/redeploy、replacement/removal、disabled policy 与 1,000-event delivered/undelivered deterministic loops；不复制机器相关一秒阈值。新增 `client.runtime.subscriber` capability、mapped case 与 12 个 Java runtime。Coverage 1638/4140 (39.57%)，214 case 中 209 mapped / 0 partial / 5 approved-difference。当前有 JDK 17 但无 Maven，未执行 Java regression suite；Java execution 以 inventory 与源测试逐项对照。本轮不需要 MySQL Docker。

本轮完整关闭 `ClientRuntimeStatementName` 4/4 execution。新增 Go-style `Engine.DeployPlans(ctx, []Plan, ...DeploymentOption)`、`WithDeploymentStatementNameResolver`、`DeploymentStatementNameContext` 与 `Deployment.Statement(name)`：一个有序 Plan slice 作为 Java compiled module 的 typed replacement，同一 deployment 内保持声明/dispatch/管理顺序，未命名 Plan 稳定分配 `stmt-0`、`stmt-1`；resolver 在 Engine lock 外取得 index、immutable Plan 和 original name，callback error、blank name 或 duplicate resolved name 会在任何 statement 注册前返回结构化错误。Engine active statement catalog 由 name key 改为 stable statement ID key，因此两个独立 deployment 可同时持有 `@name('a')` 的等价 typed Plan，两个 listener 都收到 E1；撤销第一个 deployment 后第二个继续收到 E2。Undeploy 按 ID 精确删除，Dataflow 的无 deployment-id name lookup 按最早 deployment order 返回，显式 deployment ID 仍无歧义；同一 deployment 内 name 保持唯一。multi-plan prepare 失败会清理已准备的 statement/row-recognition state，并恢复受 `IntoTable` 初始化影响的 Table mutation snapshot。`client_runtime_statement_name_parity_test.go` 逐项固定 duplicate cross-deployment、同 deployment 两个无依赖 projection、unassigned names、resolver duplicate invalid，并补充 empty plan slice、same-deployment explicit duplicate 与 resolver callback error。Java `EPCompiled` module bytecode和精确 `EPDeployException` 文本不复制。新增 `client.runtime.statement-name` capability、mapped case 与 4 个 Java runtime。Coverage 1626/4140 (39.28%)，213 case 中 208 mapped / 0 partial / 5 approved-difference。当前有 JDK 17 但无 Maven，未执行 Java regression suite；Java execution 以 inventory 与源测试逐项对照。本轮不需要 MySQL Docker。

本轮完整关闭 `ClientRuntimeStatementAnnotation` 6/6 execution。新增 `statement_metadata.go` 与 `client_runtime_statement_annotation_parity_test.go`，以 Go typed fluent metadata 替代 EPL `@` 文本和 Java annotation reflection：`StatementDescription`、`StatementTag`、`WithStatementHints`、`StatementNoLock` 与 `WithStatementAnnotation` 统一进入 Query/Plan/Statement；`Statement.Metadata`/`Annotation` 和 `Schema.Annotations`/`Annotation` 返回深复制快照，调用方修改 tag、hint 参数、数组或嵌套 annotation 不会反向修改已构建计划。custom definition 支持 marker、required/defaulted primitive/string/enum 属性、slice/array、嵌套 annotation、annotation array 和 `reflect.Type` class value，并在构造期拒绝 blank/duplicate definition、missing required、unknown/duplicate attribute value、wrong type、nil array/element、错误 nested definition 与不支持的 map/pointer 等值；`ParseAnnotationStringEnum` 以 case-insensitive canonical Go enum 替代 Java annotation-specific import/class loading。内置 Hint 覆盖 Esper `HintEnum` 的 typed kind 与 scalar/list 参数契约，unknown、缺参、多参、禁参和 blank 参数均返回显式 error。Schema `WithSchemaAnnotation` 进入环境 canonical identity，`MyAnnotationAPIEventType` map schema 仍能由普通 statement 接收事件。Java EPL/SODA 格式、parser syntax 分类、annotation proxy `equals/hashCode/toString` 与原始 class-name import 不复制；NoLock 作为明确 instruction 暴露，但 Go 保留 Engine-wide serialized transaction boundary，规则语义仍由各 fluent builder 显式表达。新增 `client.runtime.statement-metadata` capability、mapped case 与 6 个 Java runtime。Coverage 1622/4140 (39.18%)，212 case 中 207 mapped / 0 partial / 5 approved-difference。当前有 JDK 17 但无 Maven，未执行 Java regression suite；Java execution 以 inventory 与源测试逐项对照。本轮不需要 MySQL Docker。

本轮完整关闭 `ClientRuntimeSolutionPatternPortScan` 3/3 execution。新增 `client_runtime_port_scan_parity_test.go`，以 typed fluent 规则组合而非 EPL module 复现解决方案：ObjectArray `PortScanEvent` 经 `UniqueBy(src,dst,port)` + `TimeWindow(30s)` + `GroupBy(src,dst)`，用 `CountAll` 与 `WindowAccessBy[Event]` 物化到双主键 `ScanCountTable`；KeepAll `SituationsWindow` 保存首次 detection time；`TimerCron` 在每分钟边界读取当前 table/situation 状态。statement listener 在 Engine lock 外安全重入，负责首次阈值命中时创建 situation 并发送 `DETECTED`，以及分钟维护时先发送 `UPDATE`，再按 count<10 删除并发送 `DONE`，或 16 小时后发送 `EXPIRED`。三个测试固定 20 个唯一端口立即 DETECTED=20、29/59 秒同端口刷新后 8:01 UPDATE=20，以及不刷新时 8:01 的 UPDATE=0 后 DONE=0；detection count 由 typed contributor window 实际长度产生。该切片复用既有 Window/Table/Named Window/Cron/route 能力，无核心运行时代码修改。新增 `client.runtime.solution-port-scan` capability、mapped case 与 3 个 Java runtime。Coverage 1616/4140 (39.03%)，211 case 中 206 mapped / 0 partial / 5 approved-difference。当前容器无 JDK/Maven 可执行文件，Java execution 以 inventory 与源测试逐项对照；Go 全量测试、vet 与 race 门禁通过。

本轮完整关闭 `ClientRuntimeRuntimeProvider.ClientRuntimeObtainEngineWideRWLock` 1/1 direct execution。新增 Go-style `Engine.WithRuntimeWriteLock(ctx, action)`：callback 在 Engine-wide transaction mutex 下运行，因而与 send/deploy/time/state mutation 全部互斥；`defer` 保证 action 返回错误时也释放，nil action、nil/closed Engine 返回结构化错误。Go 不暴露可被长期持有或遗漏 unlock 的裸 mutex handle，且明确 mutex 非重入，callback 不得调用另一个获取同锁的 Engine 方法。`TestClientRuntimeObtainEngineWideWriteLockParity` 在持锁 callback 中阻塞，证明并发 `SendEvent` 不会提前完成；释放后 send 正常结束且 `CountAll` snapshot 为 1，并覆盖 nil action invalid。Java execution 只使用 runtime instance RW lock 的 writeLock，未触及 readLock，因此本切片不冒充共享读锁能力。全量 race 首次暴露 JMS source 测试在 `Emit` 已通知、`AckAfterProcess` 尚未落盘之间立即读取 ack 的测试时序缺口；生产顺序未改，测试改用既有 `waitJMS` 等待 ack 可见。复跑又发现 AMQP consume 在 pre-select state check 后与 `Pause` 竞争时仍可能处理刚收到的 delivery；生产 loop 现像 JMS 一样在 receive 后二次 `waitStarted`，把该 delivery 保留到 Resume。两条边界均以 race 复跑固定。新增 `client.runtime.instance-lock` capability、mapped case 与 1 个 Java runtime。Coverage 1613/4140 (38.96%)，210 case 中 205 mapped / 0 partial / 5 approved-difference。当前容器无 JDK/Maven 可执行文件，Java execution 以 inventory 与源测试逐项对照；Go 全量测试、vet 与 race 门禁通过。

本轮完整关闭 `ClientRuntimePriorityAndDropInstructions` 6/6 execution。新增普通 continuous statement 专用的 `StatementPriority(int)` 与 `StatementDrop()` fluent QueryOption，并通过 `Statement.Priority()` / `DropsLowerPriority()` 暴露 typed metadata；它与 update-istream 的 `UpdatePriority`/`UpdateDrop` 明确分层，普通 statement 按 priority 降序，同 priority 时 Drop 优先、其余保持 deployment order，update-istream 继续按既有升序 copy-on-write 预处理，Build 拒绝混用两套选项。Engine 新增独立 dispatch 排序，不改变 `Statements`/`TraverseStatements` 的 deployment-order 管理契约；外部事件、内部 routed event、virtual-clock timer、直接/statement-fed Named Window delta 均在同一规则下处理，Drop 只有在自身产出结果时才截断更低优先级，且保留自身 listener batch 与 route。`client_runtime_priority_drop_parity_test.go` 逐项复现 scheduling priority/drop、last-event Named Window 的 typed insert trigger + on-window select priority/drop、普通 SupportBean priority，以及动态 add/remove：Drop filter 抑制 insert-into ABCStream，未命中时放行，新增/撤销 l0/l1/l2 与 priority=50 statement 后顺序立即重算；同时固定 Plan canonical、typed metadata、管理 traversal 顺序和 update-istream invalid 边界。新增 `client.runtime.priority-drop` capability、mapped case 与 6 个 Java runtime。Coverage 1612/4140 (38.94%)，209 case 中 204 mapped / 0 partial / 5 approved-difference。当前容器无 JDK/Maven 可执行文件，Java execution 以 inventory 与源测试逐项对照；Go 全量测试、vet 与 race 门禁通过。

本轮完整关闭 `ClientRuntimeLockLogging` 1/1 execution。新增 `WithLockActivityTracing()`、`Engine.LockActivity()` 与 `Engine.ClearLockActivity()`：默认关闭时 Engine 仍使用零配置 mutex 包装；显式开启后以固定容量内存记录器捕获 Engine transaction mutex 的精确 `attempt`、`acquired`、`released` 转换，事件用单调 sequence 排序，快照和清空只锁独立 recorder，不递归污染被观察的 Engine lock trace，也不写进程级全局日志。`TestClientRuntimeLockLoggingParity` 用 fluent `CountAll()` statement 发送一个事件并以 `Statement.Snapshot` 对照 Java safe iterator 的 `c0=1`，同时验证三阶段存在、计数平衡、sequence 严格递增、clear 与 undeploy 生命周期。Java execution 关闭 internal timer 并开启 lock logger，但不断言日志文本；Go 默认就是无后台 timer 的显式虚拟时钟，因此只复制可观察的锁诊断能力并提供可测试内存接口。新增 `client.runtime.lock-activity` capability、mapped case 与 1 个 Java runtime。Coverage 1606/4140 (38.79%)，208 case 中 203 mapped / 0 partial / 5 approved-difference。当前容器无 JDK/Maven 可执行文件，Java execution 以 inventory 与源测试逐项对照；Go 全量测试、vet 与 race 门禁通过。

本轮完整处置 `ClientRuntimeItself` 5/5 execution。`ClientRuntimeItselfTransientConfiguration` 新增 `WithRuntimeService(name, value)` 与 `Engine.RuntimeService`，Engine 在构造时复制命名引用且之后只读，listener 通过闭包访问 `TEST_SERVICE_NAME` 并固定读取 secret=12345，不复制 Java transient configuration map/service locator 外形。`ClientRuntimeSPIStatementSelection` 新增 `Engine.Statements`、`TraverseStatements`、`StatementPredicate`、`StatementNameEquals`、`StatementDeploymentIDContains`、`StatementContains` 和 `Statement.DeploymentID`：按 deployment order 取得 detached snapshot，predicate/visitor 在 engine transaction lock 外运行，typed name filter 与 canonical Plan contains(`xxx`) 分别选中 b，deployment-id contains(`x`) 为空。`ClientRuntimeSPIBeanAnonymousType` 用无需注册到 Environment 的 `StructSchema[clientRuntimeAnonymousBean]` 固定 `prop` 为 Go `int`。`ClientRuntimeSPICompileReflective` 的平台无关可观察语义由 typed API 对照：Named Window 插入 E1 后，普通 `Environment.Build` 产生 FAF Plan、两个 live Plan 与稳定 canonical，`ExecuteFireAndForget`/Deploy/Statement Snapshot 都读回 E1，`Multiply(Literal(1), Literal(1))` 得 1；EPL parser、Module/SODA model 和 reflective compiler service 按项目无 EPL 主入口边界不复制。`ClientRuntimeWrongCompileMethod` 单列 approved difference：Java 区分 module/FAF `EPCompiled` 并拒绝交叉执行，Go 只有一个不可变 typed logical Plan，同一 read-only Named Window Plan 合法用于 FAF 与 Deploy 且结果一致，从模型上消除了错误工件类别。新增 `client.runtime.self-management` capability、1 个 mapped case、1 个 approved-difference case 与 5 个 Java runtime。Coverage 1605/4140 (38.77%)，207 case 中 202 mapped / 0 partial / 5 approved-difference。当前容器无 JDK/Maven 可执行文件，Java execution 以 inventory 与源测试逐项对照；Go 全量测试、vet 与 race 门禁通过。

本轮完整关闭 `ClientRuntimeExceptionHandler` 的唯一 direct execution `ClientRuntimeExHandlerInvalidAgg`。Java 注册 `myinvalidagg`，其 aggregation state 在 `enter` 抛出 `RuntimeException("Sample exception")`，未配置 handler 时 `sendEventBean` 必须以 `EPException` 返回；Go 用 `RegisterAggregatePluginFactory` + `PluginAggregateFactoryRef` 构造同名 typed aggregate，并在用户 factory/state 求值边界将 panic 标记为 `aggregatePluginRuntimePanic`。statement runtime 仅恢复该类型化扩展边界并返回 `ErrorState`，错误稳定包含 plugin `myinvalidagg`、statement `ABCName` 与原始 `Sample exception`；其他内部 panic 会重新抛出，不被静默吞掉。Java 源文件中另外两个由 configuration harness 单独调用的 factory-context/handler-context 场景不属于 4,140 条 runtime inventory，本 case 不冒充已覆盖，保留在 capability remaining。新增 `client.runtime.exception-boundary` capability、mapped case 与 1 个 Java runtime。Coverage 1600/4140 (38.65%)，205 case 中 201 mapped / 0 partial / 4 approved-difference。当前容器无 JDK/Maven 可执行文件，Java execution 以 inventory 与源测试逐项对照；Go 全量测试、vet 与 race 门禁通过。

本轮完整关闭 `ClientRuntimeTimeControl` 2/2 execution。新增 Go-style `Engine.AdvanceTimeSpan(ctx, target, optionalResolution)`：无 resolution 时反复定位并逐个推进到期调度，保留 Java `advanceTimeSpan` 的 listener callback 边界，1.5 秒 recurring timer 在 3.5/4.5/9/10.5 秒跨度中分别产出 1.5、3、4.5、6、7.5、9、10.5 秒独立 batch，重复目标不重复触发；带 200ms resolution 时按固定采样推进，逾期调度在实际采样时刻触发并从该时刻重锚，固定得到 12.1 与 13.7 秒。新增 `Engine.NextScheduledTime` 和 `StatementNearestSchedules`，按 active statement 汇总 pattern/output/window 及已活动 context partition runtime 的最近 engine-clock callback，返回 deployment ID、statement name 与绝对 `time.Time` 并稳定排序；undeploy 后调度立即移除。Java 非 `every` 的 `timer:interval` 是 one-shot，而 Go `TimerInterval` 是显式 recurring builder，因此最近调度测试用等价 typed `TimerAt(2s)`、`TimerAt(150ms)` 和 `TimerAt(3s).And(TimerAt(4s))` 对照。新增 `client.runtime.time-control` capability、mapped case 与 2 个 Java runtime。Coverage 1599/4140 (38.62%)，204 case 中 200 mapped / 0 partial / 4 approved-difference。当前容器无 JDK/Maven 可执行文件，Java execution 以 inventory 与源测试逐项对照；Go 全量测试、vet 与 race 门禁通过。

本轮完整关闭 `ClientRuntimeListener` 1/1 execution。新增 `client_runtime_listener_parity_test.go`，验证 statement listener 在 engine transaction lock 释放后可同步重入注入事件：trigger 收到 `SupportBean("xy")` 后依次通过 `Engine.Route`、`SendXML` 和 `JSONSender.Route` 发送 struct bean、map、ObjectArray、XML、Avro、JSON 六种表示，各自的 fluent consumer 恰好收到一个 `ident=xy`。Java 将七条 EPL 放在单 module 中，Go 按 one typed Plan per statement 分别部署；Java 预配置 XML 把 attribute 映射为 ident，Go XML schema 用显式声明的 ident child element 保持同一公共属性值。该 execution 复用既有 listener、Route 与 representation sender 能力，无运行时代码修改。新增 `client.runtime.listener-route` capability、mapped case 与 1 个 Java runtime。Coverage 1597/4140 (38.57%)，203 case 中 199 mapped / 0 partial / 4 approved-difference。当前容器无 JDK/Maven 可执行文件，Java execution 以 inventory 与源测试逐项对照；Go 全量测试、vet 与 race 门禁通过。

本轮完整关闭 `ClientRuntimeUnmatchedListener` 3/3 execution。新增 engine-wide `SetUnmatchedListener`：nil 可禁用，外部 Send 与 insert-into 内部 route 都进入同一未匹配边界；callback 在 engine transaction lock 释放后执行，因此可安全地重入 Deploy，并从下一事件起参与匹配。未匹配判定显式依据 active statement 的输入 stream filter，忽略后置 having/output suppression；输入 filter 未命中会报告原事件，statement active/redeploy 后静默，undeploy 后恢复报告并保留 underlying 指针 identity。insert-into 的源事件由 route statement 消费，未被任何 statement 消费的派生 `MyEvent` 才交付 unmatched listener；Java 源测试最后一步以注释表示 restart 但未写 deploy，Go 用 immutable Plan 的显式 redeploy 固定该生命周期意图。新增 `client.runtime.unmatched-listener` capability、mapped case 与 3 个 Java runtime。Coverage 1596/4140 (38.55%)，202 case 中 198 mapped / 0 partial / 4 approved-difference。当前容器无 JDK/Maven 可执行文件，Java execution 以 inventory 与源测试逐项对照；Go 全量测试、vet 与 race 门禁通过。

本轮完整关闭 `ClientRuntimeEPStatement` 2/2 execution。新增 Go-style `Statement.SubscribeWithReplay(ctx, listener)`：在注册 listener 的同一 statement 状态快照中计算 retained iterator view，空窗口也同步回调一个 empty `ResultBatch`，已有 1/2 个事件按窗口迭代顺序一次回放；回放期间到达或由回调重入发送的 live event 先缓冲，随后以独立 batch 有序交付。`SnapshotWithSelector` 的校验顺序改为 destroyed-state 优先，`Subscribe` 的销毁错误统一为稳定 `ErrorState`；undeploy 后 `Snapshot`、selector Snapshot、Subscribe、SubscribeWithReplay、Stop、Start 均拒绝，同时 Statement metadata 句柄和 `StatementDestroyed` 状态保留。新增 `client.runtime.epstatement` capability、`case.client-runtime-epstatement` 与 2 个 Java runtime。Coverage 1593/4140 (38.48%)，201 case 中 197 mapped / 0 partial / 4 approved-difference。当前容器无 JDK/Maven 可执行文件，Java execution 以 inventory 与源测试逐项对照；Go 全量测试、vet 与 race 门禁通过。

本轮开启 client 管理面运行态对账，完整关闭 `client/basic` 的 7 个 direct execution：`ClientBasicAggregation`、`Annotation`、`Filter`、`LengthWindow`、`Pattern`、`Select`、`SelectClause`。新增 `client_basic_parity_test.go`，全部规则用 typed fluent API 构造：`CountAll` 连续输出 1/2/3；`StatementName("abc")` 在部署后稳定可读（Java execution 只断言 Name annotation，Tag/Priority/Drop 的 annotation-array 反射不复制到普通 select）；字段过滤逐事件命中/静默；`LengthWindow(2)+WithOldStream` 的 E1/E2 新流与 E3/E4 淘汰旧流保留发送指针 identity；wildcard select 保留 underlying 指针；具名字段投影得到 intPrimitive=10；Java one-shot `timer:interval(10)` 以虚拟时钟下等价 `TimerAt(origin+10s)` 表达，因为 Go `TimerInterval` 是显式 recurring builder。新增 `client.basic` capability 与 mapped `case.client-basic`，登记 7 个 Java runtime。Coverage 1591/4140 (38.43%)，200 case 中 196 mapped / 0 partial / 4 approved-difference。All gates pass.

本轮收官 `EPLOtherSplitStream` 全部 12/12 execution，`case.epl-other-split-stream` 从 partial 转为 mapped。`TestSplitStreamSingleInsertContextParity` 对照 `create context mycontext initiated by SupportBean as criteria; context mycontext on SupportBean_S0 as event insert into ...`：链式 `CreateInitiatedContext` + `OnEvent(...).SplitFirst(SplitInto(...)).Query(WithContext(...))` 投影 `ContextID()`、`ContextInitiatingEvent()` 与当前 `EventValue[Event]()`；两个 Event 通过分析型 `Func1` 显式解包为 Go 指针写入 map schema，结果 `criteria` 与发送的 `*splitStreamSupportBean`、`event` 与发送的 `*splitStreamSupportBeanS0` 均为同一指针，投影 cid 与活动 context descriptor ID 一致。该 execution 复用既有 context/runtime/split 能力，无运行时代码改动。新增 Java runtime `java-runtime-cd8a075673144f4c4c36`；Coverage 1584/4140 (38.26%)，capability case 195 mapped / 0 partial / 4 approved-difference。All gates pass.

本轮扩展 `EPLOtherSplitStream` 至 11/12 execution，关闭 `EPLOtherSplitStreamSplitPremptiveNamedWindow`。`SplitInto` target 现可直接引用普通事件类型或 Named Window：普通流结果继续进入 routed-event 队列，Named Window branch 则在 split 语句持有引擎事务锁期间立即 materialize/insert，并通过既有 `queueNamedWindowDeltaLocked` 更新窗口消费者；因此即使 `OtherStream` branch 声明在前、`WinTwo` branch 声明在后，前者的级联 on-select 也在队列排空时看到 `WinTwo.col2=2`。校验与投影统一使用目标实际 schema，Named Window 无需额外注册同名 event type；context-scoped window 复用 `scopedForVariables`。`TestSplitStreamPreemptiveNamedWindowParity` 对照 Java 的 ObjectArray/Map/Avro/JSON/JSON-provided 循环，逐表示验证 trigger 发送、window schema kind、窗口快照和 preemptive on-select 可见性。新增 Java runtime `java-runtime-3924935c21e61187d9e3` 登记到 partial `case.epl-other-split-stream`；仅 SingleInsert execution 留待后续。Coverage 1583/4140 (38.24%). All gates pass.

本轮扩展 `EPLOtherSplitStream` 至 10/12 execution，关闭 `EPLOtherSplitStreamFromClause`。新增 `SplitFrom(source).Into(...)` / `IntoWhen(...)` branch-local 链式 API：branch source 必须派生自同一 `OnEvent` 根流，只允许无状态 source/filter/contained-event 链；Plan canonical/参数遍历纳入 branch source，Build 拒绝跨 Environment、无关根流及 stateful branch source。运行时在存在 branch source 时按分支优先遍历，`SplitAll` 保持 Begin→全部 body→End 的 routed-event 顺序，`SplitFirst` 选择首个实际产出行的分支并保留该分支全部 contained rows；投影支持 `ContainedParentField`/`ContainedParentEvent`。`TestSplitStreamFromClauseParity` 对照四组 Java 场景：Begin/body/End 对 3/1/0 items 的多行路由和字段顺序、同一 contained source 的多种父/子投影、productId 条件下 output-first where 分支选择，以及文档示例中 Begin→Item→End 队列驱动 initiated-terminated context 在终止时输出 count=1。SODA/EPStatementObjectModel 双形态由同一不可变 typed Plan 覆盖。新增 Java runtime `java-runtime-d887456a166d0ac07777` 登记到 partial `case.epl-other-split-stream`；SplitPremptiveNamedWindow、SingleInsert 两个 execution 留待后续。Coverage 1582/4140 (38.21%). All gates pass.

本轮扩展 `EPLOtherSplitStream` 至 9/12 execution，关闭两个 subquery 分流。`TestSplitStreamSubqueryParity` 对照 lastevent scalar subquery：分支 predicate 与 projection 共享 statement subquery registry，SupportBean_S0 到达前 `id/p01` 均为 Null，B 分支经 `is null` 命中并投影 null；到达 `{id:10,p00:x,p01:y}` 后 intPrimitive=10 进入 A 投影 x，intPrimitive=9 进入 B 投影 y。`TestSplitStreamSubqueryMultikeyWArrayParity` 对照 `sum(value) group by array`：`SubqueryGroupScalar[[]int,int]` 在 `[1,2]` 单组上依次返回 21/33，加入 `[1]` 第二组后按 Esper 标量多行语义返回 Null，非正 trigger 走默认 0 分支；数组键继续使用结构化分组而不要求 Go comparable。split branch 的直接 aggregate 仍 Build 拒绝，但 subquery aggregate 是合法边界。新增 2 个 Java runtime 登记到 partial `case.epl-other-split-stream`；FromClause、SplitPremptiveNamedWindow、SingleInsert 三个 execution 留待后续。Coverage 1581/4140 (38.19%). All gates pass.

本轮启动 `EPLOtherSplitStream` 套件并完成首批 7/12 execution：新增 `SplitStreamBranch`、`SplitInto`/`SplitIntoWhen` 与 `OnEvent(stream).SplitFirst(...)`/`SplitAll(...)` 链式 API，规则以类型化分支、谓词和投影构造，不接受 EPL/SODA 文本。运行时按声明顺序计算分支：`SplitFirst` 首次命中即停止，`SplitAll` 投递全部命中分支，无命中时 split 语句自身返回原事件；空投影执行 wildcard schema 投影，具名投影生成目标事件，多目标 route 进入同一 routed-event 工作队列。Plan canonical/hash 区分 first/all 和每个 target/condition/projection，参数/变量遍历纳入全部分支。Build 门禁拒绝空分支、未知目标、非 bool 条件及 aggregate projection。`epl_other_split_stream_parity_test.go` 对照完成 2SplitNoDefaultOutputFirst、Invalid、1SplitDefault、2SplitNoDefaultOutputAll、3SplitOutputAll、3SplitDefaultOutputFirst、4Split；SODA/model 形态由 typed plan 取代。新增 7 个 Java runtime 登记到 partial `case.epl-other-split-stream`；FromClause、SplitPremptiveNamedWindow、Subquery、SubqueryMultikeyWArray、SingleInsert 五个 execution 留待后续切片。Coverage 1579/4140 (38.14%). All gates pass.

本轮对照 `EPLOtherUnaryMinus` 的 1 个 execution：新增 `TestEPLOtherUnaryMinusMatchesEsper`，用链式 `Negate(Field(...))` 与 `Negate(VariableRef(...))` 复现 Java `select -intPrimitive as c0, -v as c1`；发送 `SupportBean("E1", 10)` 后固定投影 `{c0:-10,c1:-1.0}`，并验证运行时变量 `v` 仍为 `1.0`、表达式求值没有副作用。Go 使用带类型、可分析的 `Negate` 构造器，不接受 EPL 前缀字符串。新增 Java runtime `java-runtime-8c3dcb3623deea547561` 登记到 `case.epl-other-unary-minus` 并映射到 `expr.core`。Coverage 1572/4140 (37.97%). All gates pass.

本轮收官 EPLOtherUpdateIStream 套件至 27/27 execution（case.epl-other-update-istream 由 partial 转为 mapped，全仓 partial case 归零）：落地命名窗口 update-istream、bean 类型继承 target 与 listener 路由预处理三个 execution。EPLOtherUpdateNamedWindow 对照 update istream 作用于命名窗口——Esper 把 update strategy 绑到窗口插入路径：Go 在 insertWithVariables 内新增引擎级预处理钩子（applyNamedWindowUpdatesLocked/windowUpdateStatementsLocked，按优先级升序、drop 优先的平局规则链式应用，复用 processUpdateStream 全程），被插入事件先经 copy-on-write 更新再进窗口，窗口内容、窗口监听、select/on-select/on-insert 消费者与 update 语句自身 IR 对全部观察更新副本，而喂入的 insert/on-trigger 语句发布更新前行（trigger.go 改为以 newInsertEvent 的 offered 事件组装 mutation 结果）；窗口目标 update 语句不再消费普通流事件（statement.process 跳过）也不作为订阅者消费窗口 delta（processNamedWindow 跳过），匹配 Esper 的绑定语义；@Drop 命中时事件在进入窗口前移除；Engine.InsertNamedWindowInModule 的待派发队列重置前移到插入之前以便 update 批次同周期派发；Esper milestone serde 检查点无 Go 对应。EPLOtherUpdateInsertDirectBeanTypeInheritance 对照 bean 类层级（create schema as <class> 接口与子类 targeting）：以 map schema + WithSchemaParent 链建模（bean 类继承无 Go 对应），超类型 update 经传递性 schema-parent 走查作用于子类插入（BaseInterface→BaseOne→BaseOneA/BaseOneB、BaseInterface→BaseTwo），@Priority 跨层级排序（0/2/3 依次覆盖得 BLANK/FINAL），select-from-supertype 消费者同步观察更新副本；既有 acceptsEventType 父链机制零运行时代码改动完成对照。EPLOtherUpdateSendRouteSenderPreprocess 对照 listener 内 routeEventMap/routeEventBean：Engine.Route（Send 的重入别名，listener 在引擎锁释放后派发故可安全重入）路由的 map 与 bean 事件与外部发送事件一样经过 update-istream 预处理与 @Drop 移除，零运行时代码改动完成对照；Esper 路由在当前工作单元内的排队时序不单独断言（Go 在 listener 回调内同步处理、外层 send 返回前完成）。新增 3 个 Java runtime 已登记；case.epl-other-update-istream 转为 mapped（27/27）。Java TestSuiteEPLOther 27/27 通过。Coverage 1571/4140 (37.95%). All gates pass.

本轮扩展 EPLOtherUpdateIStream 切片至 24/27 execution：落地 update-set 赋值类型校验全矩阵与枚举 anyOf where 门控。EPLOtherUpdateInvalid 对照非法矩阵——八类拒绝全部在 Build 期触发：收窄数值赋值（long→int，类型不匹配）、未知属性、null 字面量赋不可空属性（可空性不匹配）、点号 tagged 属性、聚合函数进 set、聚合函数进 where、Prev 进 set、未知事件类型（XYZ.GYH）；两个正对照保持接受——加宽数值赋值（int→long）与 null 字面量赋可空属性（*int64）。EPLOtherUpdateIStreamEnumAnyOf 对照枚举方法进 update where：EnumAnyOf[any](Field mylist, Not(IsNull[any](EnumElement[any]()))) 门控集合属性含非 null 元素的事件，命中后 updated 置 true。Go 侧新增 validateUpdateSetAssignable 与 updateSetWidensTo 助手并接入 validateUpdateStream 的纯赋值分支（无索引/无 map key）：对齐 Esper 写访问规则——类型可赋值或仅允许加宽方向数值（拒绝 long→int 收窄；Go 侧将 int 秩定为 3、int64 秩定为 4 以镜像 Esper int/long 语义，尽管二者同为 64 位），指针类型在两侧均解引用后比较；null 字面量（node.kind=="null"）赋不可空属性在 Build 期以可空性不匹配拒绝，而表达式求值产生的 null 仍保持运行时静默跳过语义；无类型 any 表达式/目标放行到运行时。四个 Esper 专属拒绝类（只读 bean 属性、不可拷贝 bean、XML 写访问、未加前缀外部引用）在 Go 模型下无对应，记入已批准差异。新增 2 个 Java runtime 已登记；case.epl-other-update-istream 保持 partial（24/27）。Java TestSuiteEPLOther 27/27 通过。Coverage 1568/4140 (37.87%). All gates pass.

本轮扩展 EPLOtherUpdateIStream 切片至 22/27 execution：落地 update-set/where 子查询求值。EPLOtherUpdateSubquery 三部分对照——(1) 非关联标量子查询进 set（theString = lastevent 子查询，空子查询向可空属性写 null，E2={null,1}）加 IN-子查询 where（w0 keepall 快照门控），lastevent 语义经 s0 序列 {E1,0}→{null,1}→{E3,2}→{newvalue,1}→{othervalue,1} 逐步对齐；(2) 关联子查询 set intPrimitive = keepall 子查询 where s0=OuterField(theString)，null 结果在不可空 int 属性上跳过写入保持原值（E6={E6,8}），命中后赋值（E7={E7,91}）；(3) Esper as-mystream 别名变体以全新等价 plan 镜像 redeploy 边界（E8 跳过、E9={E9,-1}），别名本身无 Go 对应记入差异。EPLOtherUpdateSubqueryMultikeyWArray 对照分组标量子查询（sum(value) group by 数组 multikey）：单组赋聚合值（21→33），多组赋 null——Esper 标量分组子查询多行语义经新增 SubqueryGroupScalar 表达式落地（零组/多组均 Null，数组等非可比较键不经 Go map 键）。Go 侧：validateUpdateStream 移除 set/where 子查询整段拒绝（复用既有 validateSubquery 全链路校验；数组索引/map key 内的子查询仍拒绝），聚合检查天然不下钻子查询节点（children=nil）故分组内 Sum 不被误拒；processUpdateStream 的 evalContext 绑定 Engine（子查询快照）与 OuterEvent=更新前事件（关联作用域对齐 Esper 的 ABCStreamSQ.theString）。新增 2 个 Java runtime 已登记；case.epl-other-update-istream 保持 partial（22/27）。Java TestSuiteEPLOther 27/27 通过。Coverage 1566/4140 (37.83%). All gates pass.

本轮扩展 EPLOtherUpdateIStream 切片至 20/27 execution：以等价表示 parity + 已批准差异方式登记五个表示/模型面 execution，无运行时代码变更。EPLOtherUpdateSODA：运行时形状（map 类型 update istream + where）以链式 API 对照；SODA 对象模型面（EPStatementObjectModel/toEPL/eplToModel 往返）与 update 可选流别名（as mytype）在 Go 不可变链式 Plan 下无对应，记已批准差异。EPLOtherUpdateXMLEvent：XML DOM 表示无 Go 对应，以 map 表示对照 insert-into 追加两个字面量列（valOne/valTwo）+ update where prop1 匹配改写两列（{987,123,SAMPLE_V1}）。EPLOtherUpdateWrappedObject：Esper 把 bean 作为 fragment 与额外列包装，Go 以等价扁平隐式流对照三种 update 形状（仅额外列/仅源属性/混合）与 undeploy/redeploy 边界（E1={987,123,E1}、E2={1,2,A}、E3={555,2,B}）。EPLOtherUpdateCopyMethod：Esper 需用户 copy method/序列化器支撑 bean copy-on-write，Go struct 天然逐字段拷贝无需钩子，运行时形状对照（{x,y}）。EPLOtherUpdateExpression：inlined_class Helper.swap(me) 为 JVM 专属且 set-方法调用 形式无 Go 对应；可观察的 map 交换语义由 Go 全 set 表达式对更新前事件求值规则天然达成（set a=b, b=a 得 {10,1}），记已批准差异。测试侧补全 insert-into 目标需预注册的惯例（RegisterMap 隐式流目标/RegisterStruct bean 通配路由），RecordStream.Select 方法承载无类型源投影。新增 5 个 Java runtime 已登记；case.epl-other-update-istream 保持 partial（20/27）。Java TestSuiteEPLOther 27/27 通过。Coverage 1564/4140 (37.78%). All gates pass.

本轮扩展 EPLOtherUpdateIStream 切片至 15/27 execution：落地 named-window on-trigger 嵌套写入与 array-element 三件套。EPLOtherUpdateNWSetMapProps 对照 on SupportBean update 命名窗口逐行写 simple/mymap('abc')/myarray[2]——row1 全量应用（E1 后 {A,10,10}，E2 再触发更新为 20），row2 的过小数组与 null map 被静默跳过而 simple='A' 仍应用；SERDEREQUIRED（HA serde 往返）无 Go 对应。EPLOtherUpdateArrayElement 对照索引表达式读取事件属性（intarray[position]=1 与 objectarray[position]=1 单语句双数组写），消费者观察更新副本的变异数组（position=1/0/2 三轮）。EPLOtherUpdateArrayElementBoxed 对照算术索引（dbls[3-2]=1）与 int 字面量向 boxed double 元素（[]*float64）的拓宽再装箱。EPLOtherUpdateArrayElementInvalid 对照非法矩阵（未知属性/null 索引表达式/值类型不兼容/非数组目标/未知索引字段全部 Build 拒绝，Go 诊断文案与 Esper 原文不同但拒绝形状一致）与运行时三态（越界索引以 Esper 诊断原文 Array length N less than index M 报错、null 索引静默跳过、primitive 数组 null rhs 静默跳过）。Go 侧关键语义经 Esper 源码与 InfraNWTableOnMerge 双重实证后落地：越界行为按目标分型——纯 named-window on-trigger update 静默跳过（lenientIndexBounds 仅传入 updateWhere 行更新回办），table update/merge/FAF/update-istream 一律以 Esper 诊断原文报错；trigger.go 的 evaluateTriggerAssignmentsForTarget 全 12 个调用点逐一标注，既有 table 越界报错测试保持通过；on-trigger 路径启用 map-entry 赋值（Key 校验+执行，复用 update-istream 克隆写语义），validateTriggerAssignmentType 放开数值向 boxed 数值目标赋值（与运行时 widen 对齐），嵌套写入统一先经 coerceUpdateSetValue 预 coerce 再走 assignReflectValue（修复 int 到 *float64 元素的装箱间隙）。parity 测试以 OnEvent(...).InsertIntoNamedWindow + FromNamedWindow(...WithOldStream()) 惯例建模窗口填充与 IR 观察。新增 4 个 Java runtime 已登记；case.epl-other-update-istream 保持 partial（15/27）。Java TestSuiteEPLOther 27/27 通过。Coverage 1559/4140 (37.66%). All gates pass.

本轮扩展 EPLOtherUpdateIStream 切片至 11/27 execution：落地嵌套 map-entry 与 array-element 写入。EPLOtherUpdateMapSetMapPropsBean 对照 bean 事件的 props('abc')=1 与 array[2]=10 嵌套写入（经 insert-into 路由），IR 对 new 读到 {1, 10}、old 读到 {null, null}，且发送 bean 的嵌套容器全程不被原地修改。EPLOtherUpdateMapSetMapPropsRep 对照 map 表示（Java 对四种事件表示循环，Go 以 map 单遍覆盖）：update 直接作用于 MyInfraTypeWithMapProp，set simple='A', mymap('abc')=1, myarray[2]=10，IR 对 new={A,1,10}、old={null,null,0}。Go 侧：trigger.go 的 TableAssignment 新增 Key 字段与 SetMapEntry(column, key, expr) 链式构造器（key/value 保持可分析表达式而非字符串规则），on-trigger 与 named-window insert-from 路径显式拒绝 map-entry 赋值（待 named-window 切片启用，避免静默忽略）；plan.go 的 validateUpdateStream 把索引赋值的整段拒绝改为完整写访问校验——索引表达式须返回整数、目标须为 array/slice、值须可赋给元素类型、map key 表达式须返回 string、目标须为 string-key map，重复属性规则按完整目标身份（列+索引/key 描述）判定使 arr[0] 与 arr[1] 不互斥；runtime.go 新增 applyIndexedUpdateSetValue/applyKeyedUpdateSetValue——写入前克隆目标容器保住 copy-on-write（同一列多次写在上一克隆上递进），null 容器/索引/key 静默跳过（对齐 Esper null-index 与 null-array 行为），越界索引以 Esper update-istream 诊断原文报错（Array length N less than index M for property 'x'），null rhs 对可空元素类型写零值、否则跳过（对齐 Esper 对 primitive 数组 null rhs 的跳过语义）。parity 测试断言嵌套容器的新旧值与发送侧未变异，并以四个 Build 拒绝覆盖 EPLOtherUpdateArrayElementInvalid 的 Go 可强制子集（完整非法矩阵与越界运行时行为随 array-element execution 后续登记）。新增 2 个 Java runtime 已登记；case.epl-other-update-istream 保持 partial（11/27）。Java TestSuiteEPLOther 27/27 通过。Coverage 1555/4140 (37.56%). All gates pass.

本轮扩展 EPLOtherUpdateIStream 切片至 9/27 execution：落地 @Priority/@Drop 注解与多 update 排序对照。EPLOtherUpdateFieldsWithPriority 对照八条优先化 update 与一条 drop 链（Java 对四种事件表示循环，Go 以 MyStream map 表示单遍覆盖）：priority 升序应用使高优先级赋值胜出（A1 经 g(6)/f(7)/d(8)/c(9)/e(10) 得 10），平局时 drop 条目先行、其后按 deployment 稳定序；每条 update 的 where 子句观察前一 update 留下的运行副本（D1(100) 时 b(11) 先置 -1、a(12) 观察到 -1 再置 -2）；@Drop 把事件整个移出分发周期（B1 被 h drop，s0 无交付）且 drop 语句本身不交批；undeploy e 后次高优先级 c(9) 胜出（Z2=AXZ2,9），再 undeploy c/d/f/g 后无 update 匹配、值原样透传（Z3=AXZ3,0）。EPLOtherUpdateUnprioritizedOrder 对照无优先级 update 按 deployment 序应用、最后赋值胜出（A/B/C/D 得 D）。EPLOtherUpdateListenerDeliveryMultiupdate/Mixed 对照每条 update 的 insert/remove 对看到前一 update 的运行副本（B old=经 A 更新的副本 {A,1,a}）、where 命中的语句才交付 IR 对、消费者 s0 观察最终赋值，Mixed 变体五条无优先级 update 中仅 B/D 挂监听器。Go 侧：stream.go 新增 UpdatePriority(n) 与 UpdateDrop() 两个 QueryOption（UpdateStreamQuery.Query 让全部选项作用于同一共享 querySpec，修复逐选项新建 spec 导致语句名被自动命名覆盖的冲突）；runtime.go 的 orderUpdateStatementsFirst 把 update 组内排序从纯 deployment 序改为 (priority 升序, 平局 drop 优先, deployment 稳定序)，新增 Statement.droppedEvent/takeDroppedEvent 机制——引擎分发循环在 update 命中 drop 时终止该事件的后续语句分发。parity 测试的 updateIStreamCapture 扩展 newResults/oldResults 原始 Result 收集（insert 语句带 Alias 投影交付 Row 而非 Event），Multiupdate/Mixed 断言改走 Result.Get 统一面。差异备注：drop 紧随先前匹配 update 时 Esper 抑制先前已排队 indicate，Go 仍交付已入队批（套件中不可观察的边角，已记入 manifest difference）。新增 4 个 Java runtime 已登记；case.epl-other-update-istream 保持 partial（9/27）。Java TestSuiteEPLOther 27/27 通过。Coverage 1553/4140 (37.51%). All gates pass.

本轮扩展 EPLOtherUpdateIStream 切片至 5/27 execution：对照 insert-into wildcard bean/map 路由与类型拓宽。EPLOtherUpdateInsertIntoWBeanWhere 全矩阵落地——双 update 链（update_1 where intPrimitive=1、update_2 set intPrimitive=intPrimitive+1000 where intPrimitive=2，后者部署在消费者 s0 之后仍先预处理，探针实证 Esper 路由器在事件入流时应用 update 而与部署先后无关）、链式 update 见到前一副本、undeploy/redeploy 边界切换、update_3 set intPrimitive=intBoxed（null）时 null 写入被跳过且 IR 对照常触发。Go 侧为此把两个引擎分发循环改为 orderUpdateStatementsFirst（update 语句组内 deployment 序优先于全部消费者），并在 processUpdateStream 先以 coerceUpdateSetValue 归一化 typed-nil 指针再做 null 跳过（此前 boxed nil 会被写成零值）；新增 RecordStream.UpdateStream 支撑 map/object-array 无类型流。EPLOtherUpdateInsertIntoWMapNoWhere 对照 map 类型 p0/p1 交换（双 set 均对更新前事件求值）与 undeploy/redeploy 三事件矩阵。EPLOtherUpdateTypeWidener 对照 boxed int→boxed long 拓宽与 intBoxed=null 可空写入，拓宽由 coerceUpdateSetValue 解引用-数值转换-再装箱实现。差异备注：update 语句 iterator 面（Esper 为空）与 subscriber SPI 无 Go 对应，记入 manifest remaining。新增 3 个 Java runtime 已登记；case.epl-other-update-istream 保持 partial。Java TestSuiteEPLOther 27/27 通过。Coverage 1549/4140 (37.42%). All gates pass.

本轮新增 EPLOtherUpdateIStream 切片至 2/27 execution（新功能首切）：从零落地 update-istream 语句类型。Java 探针实证 Esper 的 InternalEventRouter 预处理为 copy-on-write——源语句（Insert）监听器看到原始事件（同实例），update 监听器收 new=更新副本/old=原始事件 IR 对，下游消费者（Select）看到更新副本，原始 bean 全程不被原地修改（E2 发送后 theString 仍为 E2）；FieldUpdateOrder 语义为全部 set 表达式先对更新前事件求值再统一应用（set intPrimitive=myvar, intBoxed=intPrimitive 得 {10, 1}）。Go 侧新增链式 API From[T](env, X).UpdateStream(SetColumn(...), ...).Where(...).Query(...)（赋值复用 on-trigger 的 TableAssignment/SetColumn 惯例），plan.go 新增 validateUpdateStream（目标限普通过滤事件流、禁数据窗口/聚合/子查询/previous-prior/重复属性/未知属性/空赋值，resultSchema 返回输入流 schema），runtime.go 新增 Statement.replacedEvent 写回机制——两个引擎分发循环在 update 语句处理后以更新副本替换当前事件继续后续语句分发（deployment 序即 Esper priority 默认序），statementRuntime.processUpdateStream 以 mergeSchemaUnderlying 复制 struct/map underlying 并生成 IR 批。差异备注：Go 监听器批交付按 deployment 序（Insert→Update→Select），Esper 路由器预处理使 update 回调先于 Insert 自身监听器，逐语句批内容一致；非布尔 where 在类型化 Go builder 中编译期不可能（Where 只收 Expression[bool]）。对照覆盖 EPLOtherUpdateBean（insert-into 链三语句矩阵+实例隔离断言）与 EPLOtherUpdateFieldUpdateOrder（变量 myvar+原始值读取），并含 Go 可强制非法子集（未知属性/窗口目标/聚合 set/重复属性/空赋值）五个 Build 拒绝。新增 2 个 Java runtime 已登记到新 case.epl-other-update-istream 与 epl.other.update-istream（status partial）。Java TestSuiteEPLOther 27/27 通过。Coverage 1546/4140 (37.34%). All gates pass.

本轮收官最后两个 partial case（manifest case 层清零）：case.query-fire-and-forget-subquery（InfraNWTableFAFSubquery 20/20）与 case.query-subquery-index-sharing（InfraNWTableSubqCorrelIndex 20/20）经逐项复审后转为 mapped。两个套件的全部 Java runtime execution 此前均已登记并具备 Go 处置——FAF 子查询覆盖前 15 个功能 execution（typed SubqueryValue/SubqueryGroupRows、OuterField 关联、Named Window/Table 快照、on-demand insert/update/delete、context 局部查找、内层 where/group-by 与三条 FAF 专属非法约束）加性能/索引切片的可观察关联等值/IN 与等值前缀+B-tree 范围候选；索引共享覆盖全 20 行断言矩阵（namedWindow/table x index-share-create x disable-consumer x explicit-index x noindex、multiple-index-hints、确定性 composite/range 选择与 multikey 数组键）。复审结论：两案剩余项均为永久非目标而非可移植工作——Java 套件本身不含任何 historical/method 源 execution（源码零提及），bean 之外的事件表示组合由 event.* capability 各自追踪，JVM query-plan 钩子、Esper 完整 cost/selectivity 规划模型、10k/1M 性能阈值、事务并发快照语义与 Java 诊断原文均无 Go 对应面；以上逐条写入 manifest remaining 作为非目标备注，difference 同步改写为永久非目标表述。广义 capability query.fire-and-forget 与 query.subquery-basic 覆盖范围远超这两个套件，保持 partial 不变。无新增 Java runtime、无代码变更；至此 196 个 capability case 全部具备处置（192 mapped、4 approved-difference、0 partial）。Java TestSuiteInfraNWTable 26/26 通过。Coverage 1544/4140 (37.29%). All gates pass.

本轮收官 EPLSubselectUnfiltered 切片至 19/19 execution：落地 group-by/order-by 子查询构建期拒绝，对齐 EPLSubselectInvalidSubselect 的 group by id + (select id from SupportBean_S1) 与 order by (select id from SupportBean_S1) asc 两条拒绝路径。Go 侧 validateAggregate 的 group-by 键循环与 validateQueryModifiers 的 order-by 键循环新增 expressionNodeContainsSubquery 检查，且先于字段/子查询校验触发——即使内层子查询无窗口也报 subselects not allowed within group-by / order-by clause，与 Esper 先于子查询规划报告该诊断的顺序一致。group-by 拒绝限定在普通单源聚合：TestJoinAggregateCorrelatedSubqueryCarriesTupleScope 覆盖的 join 聚合关联子查询分组键（JoinField 携带 join 元组作用域）属 Go 有文档扩展，予以保留并写入 manifest difference；order-by 拒绝则为全形态通用（rowRecog/join/aggregate 分支之前拦截）。TestSubselectUnfilteredInvalidSubselectParity 扩展 assertInvalidMessage 助手断言错误文案，新增 group-by-subselect 与 order-by-subselect 两个拒绝用例（均使用与 Java 相同的无窗口内层形状）。无新增 Java runtime（EPLSubselectInvalidSubselect 已登记）；至此 EPLSubselectUnfiltered 19 个 execution 全部具备 Go 处置，case.epl-subselect-unfiltered 与 epl.subselect.unfiltered 转为 mapped。Java TestSuiteEPLSubselect 19/19 通过。Coverage 1544/4140 (37.29%). All gates pass.

本轮收官 EPLSubselectFiltered 切片至 27/27 execution：登记最后一个未覆盖 execution EPLSubselectSelectWildcardNoName 为 approved difference。Java 语句 select (select * from SupportBean_S1#length(1000)) from SupportBean_S0 中 Esper 对未加别名的子查询投影自动命名列 subselect_1（类型为 SupportBean_S1 bean，assertSame 原事件）；Go 侧链式 API 在构建期强制显式投影别名（plan.go resultSchema 报 projection alias cannot be blank），属显式别名边界差异。运行时交付语义与已覆盖的 EPLSubselectSelectWildcard（别名 events1）完全共享：新增 TestSubselectFilteredSelectWildcardNoNameParity 同时断言空别名 Build 报错、命名别名 subselect_1 时 ResultSchema 字段类型为 SupportBean_S1、以及 S1(-1,Y) 后 S0(0) 触发交付一行 bean 结构。manifest 登记 java-runtime-a1db086132e38b79cc29（ordinal 8），tags 增加 auto-name 与 approved-difference，difference 附自动命名说明，remaining 保留 auto-name 备注。至此 EPLSubselectFiltered 27 个 execution 全部具备 Go 处置，case.epl-subselect-filtered 与 epl.subselect.filtered 转为 mapped。Java TestSuiteEPLSubselect 19/19 通过。Coverage 1544/4140 (37.29%). All gates pass.

本轮扩展 EPLOtherDistinct 切片至 20/20 execution（套件收官）：落地 every-distinct pattern 连接对照，并修正 Go pattern 引擎的嵌套 every 腿序列语义。EPLOtherDistinctWildcardJoinPatternOne/Two 对照 select distinct * from SupportBean(intPrimitive=0) fooB unidirectional inner join pattern[every-distinct(fooA.theString) fooA=SupportBean(intPrimitive=1) -> every-distinct(wooA.theString) wooA=SupportBean(intPrimitive=2) where timer:within(1 hour)]#time(1 hour) on fooB.longPrimitive=fooWooPair.fooA.longPrimitive（Two 另带 order by wooA.theString asc 与 SupportSubscriberMRD 多行订阅）。Java 探针实证 JoinPatternOne 驱动后恰好交付 1 批 6 行 {E1,E1},{E1,E2},{E2,E2},{E1,E3},{E2,E3},{E3,E3}——followed-by 内每条 every-distinct 腿保持常驻：已捕获 fooA 的分支与之后每个 distinct wooA 配对，每个新 distinct fooA 另起分支。Go 侧修正三处 pattern 语义：(1) 序列 phase-0 扇出——左腿 every 触发时在推进分支之外保留 phase-0 续存（此前左腿每事件只产单一推进状态，多 fooA 分支无法并发）；(2) 序列 phase-1 续存——右腿 every 完成时保留 phase-1 续存（左 tag 保留、右腿复位 fired 标记但保留 distinct keyspace），匹配 Esper followed-by 的重复右腿逐 distinct 配对；(3) 完成永久性——patternRepeatingLegAlive 让含 every 腿的序列/and/within 完成不再视为终态（isQuitted=false），pattern 不再首发即停。另修复序列推进门控：every 腿状态的 done 标记在上次触发后保留，序列推进改为只认本次事件的新鲜完成（leftTransition/rightTransition.complete），此前陈旧 done 导致每次非匹配事件都以陈旧 tag 伪造推进并丢失 phase-0 监听哨（探针实证 E2(1) 无匹配时 fooA=E2 分支从未生成、E2(2) 重复交付 {E1,E2}）。SupportSubscriberMRD Object[][] 订阅面（OBSERVEROPS 标志）以 listener ResultBatch 批模型断言，无独立 subscriber SPI，env.milestone HA 检查点无 Go 持久化面对应，均记录进 manifest remaining。新增 2 个 Java runtime 已登记；至此 EPLOtherDistinct 20 个 execution 全部具备 Go 处置，case.epl-other-distinct 与 epl.other.distinct 转为 mapped。Java TestSuiteEPLOther 27/27 通过。Coverage 1543/4140 (37.27%). All gates pass.

本轮扩展 EPLOtherDistinct 切片至 18/20 execution：登记 bean event wildcard SODA 对照为已批准差异。EPLOtherBeanEventWildcardSODA 分两部分——(a) 运行时语义 select distinct * from SupportBean_A#keepall 经 E1/E2/重复 E1 发送的迭代器去重序列（{E1}→{E1,E2}→重复后保持 {E1,E2}），与既有 TestDistinctWildcardParity 的事件序列和断言逐一致，无需新增 Go 代码即覆盖；(b) SODA 部分 env.eplToModel 解析 + EPStatementObjectModel.toEPL 往返及程序化建模——EPL 文本/对象模型 API 在 Go 不可变链式 Plan 下按设计无对应，登记为 approved difference。新增 1 个 Java runtime 已登记到 case.epl-other-distinct（status partial；JoinPatternOne/Two every-distinct pattern 连接仍为后续切片）。Java TestSuiteEPLOther 27/27 通过。Coverage 1541/4140 (37.22%). All gates pass.

本轮扩展 EPLOtherDistinct 切片至 17/20 execution：落地批窗口连接 distinct 对照，并把连接元组 diff 重构为 lineage 键控。EPLOtherBatchWindowJoin 对照 select distinct theString, intPrimitive from SupportBean#length_batch(3) a, SupportBean_A#keepall b where a.theString=b.id——Esper 按每个到达/离开事件逐事件组合连接 IR，批窗口翻转在同一周期内替换同内容行时仍产出完整 old+new 对，distinct 再按批内首见 key 折叠（batch1 交付 {E1,1},{E2,2}；batch2 交付 {E2,2},{E1,1}；batch3 交付 {E2,3}，逐批有序断言）。此前 Go 的 updateJoin/expireJoin 用 before/after 内容多重集 diff（sameEvent = TypeName + DeepEqual），同内容替换在净差中丢行（探针实证 batch2 只交付 3 行连接结果中的 1 行）。修复：runtime.go 连接元组身份改为 lineage 键控——joinKeyedTuple 以各侧 join lineageID 合成键，joinTuples 全分支经 joinKeyedTuples 携带键（outer 填充位为零值、chained/多源笛卡尔各持存储行），updateJoin/expireJoin 走 diffJoinKeyedTuples，批翻转同内容替换因此产出完整 old+new；整体重建的历史/方法侧（updateJoin 与 updateUnidirectionalJoin 两条路径）在内容相等时经 assignJoinLineageIDs 保留旧 lineage，未变化的 re-poll 仍净差为零（from-clause-method 两个回归测试实证：1s3h 链式历史侧重复触发不再重发、outer-2s1h 从属历史侧不再产生多余 new 行）。dataflow select-join 状态无 lineage 追踪，保留内容多重集 diffJoinTuples 并注明限制。新增 1 个 Java runtime 已登记到 case.epl-other-distinct（status partial；JoinPatternOne/Two every-distinct pattern 连接与 BeanEventWildcardSODA 仍为后续切片）。Java TestSuiteEPLOther 套件通过。Coverage 1540/4140 (37.20%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 58/58 execution（套件收官）：落地 bean-contained 与 bean-schema-backed 两个对照。InfraBeanContained 对照 create window MyWindowBC#keepall as (bean SupportBean_S0) + insert select bean.* as bean——窗口 schema 为单 fragment 属性的 bean 容器，Go 以单 bean 字段 struct 建模、EventValue[T]() 把整个源事件作为 fragment 赋值，create 监听经嵌套 struct 路径导航 bean.p00="E1"；Esper 的 object-array/map/default rep 矩阵选择复合 underlying（Go 保持单一 struct 表示）、Avro 形态因 bean 属性无 Avro 映射在编译期拒绝（Go Avro schema 为摄入期归一化的记录字段声明，无构建期对应），两处差异已记录。InfraBeanSchemaBacked 对照 create schema ABC as SupportBean.class + ABC 窗口——类别名事件类型以同字段独立 struct 建模（一个 Go struct 对应一个事件类型，匹配 Esper 类型身份：SupportBean 发送不会路由到 ABC 别名消费；同 struct 二次注册会重指索引的隐患已在差异中声明），FAF 查询窗口首行事件类型名 "MyWindowBSB" 且持有别名字段 underlying，s0=select * from ABC 在两次 SupportBean 发送后保持静默。至此 InfraNamedWindowViews 58 个 execution 全部具备 Go 处置（逐 execution parity 测试 + 已批准差异记录），case.infra-namedwindow-views 与 infra.namedwindow.views 转为 mapped（remaining 保留共享 parity trace、复合表示矩阵与事件类型元数据面），javaNames 同步至全量 58 个。新增 2 个 Java runtime 已登记。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1539/4140 (37.17%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 56/58 execution：落地 bean-backed 窗口类型名对照。InfraBeanBacked 对照 create window MyWindowBB#keepall as SupportBean 在 object-array/map/default/Avro 四种 @EventRepresentation 下的行为——该注解对 create-window-as-bean-type 是惰性的（四种 rep 都断言 BeanEventType + SupportBean underlying），Go 一遍覆盖矩阵；核心断言是窗口事件类型名——create 监听、普通消费语句与 on-update 触发器批次投递的事件都以窗口名 "MyWindowBB" 为事件类型且持有 bean underlying（Go 既有行为：insert/update 路径把事件 typeName/streamType 置为窗口名），更新后 theString="s" 同步生效。Esper 的 BeanEventType/EventTypeTypeClass.NAMED_WINDOW 元数据与 isStatelessSelect SPI 标记无 Go 公开 API 对应（以类型名 + bean underlying 为断言代理），已记录进 manifest difference。新增 1 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；bean-schema-backed/contained 仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1537/4140 (37.13%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 55/58 execution：对照 object-array 窗口的 stream-star 插入并登记为已批准差异。InfraSelectStreamDotStarInsert 对照 @EventRepresentation(objectarray) create window MyNWWindowObjectArray#keepall (p0 int) + insert into ... select intPrimitive as p0, sb.* as c0 from SupportBean as sb——Java 探针实证 Esper 编译放行 sb.* as c0 且在插入期静默丢弃该列（object-array 行只保留 p0）。Go 类型化链式 API 按设计校验插入列必须存在于窗口 schema，未声明的 c0 在 Build 期 ErrorUnknownName（approved difference，比 Esper 更严的列校验）；可表达的 p0-only 形式与 Java 运行观测逐步对齐（窗口保留唯一列 p0=5，事件属性集 [p0]）。新增 1 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；bean-backed/schema-backed/contained 仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1536/4140 (37.10%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 54/58 execution：落地 bean 深层超类型插入对照。InfraDeepSupertypeInsert 对照 create window MyWindowDSI#keepall as select * from SupportOverrideBase + insert into MyWindowDSI select * from SupportOverrideOneA——SupportOverrideOneA("1a","1","base") 的 Esper 可见面只有 val：Java bean 层级每一级都重写 getVal()（OneA 返回 valOneA、One 返回 valOne、Base 返回 val），valOne/valBase 构造状态无 getter 对 Esper 不可见；窗口迭代器经虚分派读到最派生重写值 "1a"。Go 侧按链式 API 惯例建模：扁平 struct 仅暴露 Esper 可见的 val 属性（nwViewsOverrideBase/nwViewsOverrideOneA 各持 val），深层超类型窗口插入用显式 SetColumn("val", Field[...]("val")) 表达——Go struct schema 不能经 WithSchemaParent 继承父字段（重复字段被拒绝，schema 继承面向 Map/JSON/ObjectArray），该差异已记录进 manifest；观测行为与 Java 逐步对齐（插入成功、窗口 val="1a"）。新增 1 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；bean-backed/schema-backed/contained 与 select-stream-dot-star 仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1535/4140 (37.08%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 53/58 execution：落地命名窗口相交视图栈（intersection）对照。InfraIntersection 对照 create window MyWindowINT#length(2)#unique(intPrimitive)——state.go 新增 CompositeWindowSpec 命名窗口保留：NewNamedWindowDefinition 校验仅允许 IntersectWindowMode、至少两个子视图且子视图限 keep-all/length/unique（其余构建期 ErrorInvalidRule），namedWindowRuntime 新增 compositeChildren 按子视图各持独立保留状态（子定义克隆、索引剥离）。插入路径镜像 Esper IntersectDefaultView/IntersectAsymetricView 语义：每个子视图独立保留，窗口内容取子视图内容交集，任一子视图淘汰的事件随本次插入 delta 作为 old data 离开（E3 触发 length 淘汰 E1、unique 替换 E2，irstream 消费收到 new{E3} old{E1,E2} 任意序），不对称子视图（FirstUnique）拒绝的插入不产生 new data。删除/更新语义经 Java 探针实证后落地：on-delete 把窗口内容从每个子视图移除（removeFromNamedWindowCompositeChildrenLocked 接入 deleteWhereState）；on-update 以原始行为 old data、替换行为 new data 投递（新增 updateCompositeWhereState 按「旧事件出全部子视图、替换事件进全部子视图、重算交集」处理，更新后子视图状态与后续插入保持一致——探针序列 E4/E5→UPD→E6 逐步对齐 new=[UPD:1,UPD:3] old=[E4:1,E5:3]、new=[E6:3] old=[UPD:1,UPD:3]）。新增 1 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；bean-backed/schema-backed/contained/deep-supertype 与 select-stream-dot-star 仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1534/4140 (37.05%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 52/58 execution：落地 on-insert preemptive 双窗口级联对照。InfraOnInsertPremptiveTwoWindow 对照一个 TypeTrigger 同时触发 insert-otherstream（on TypeTrigger insert into OtherStream select col1 from WinOne）与 insert-window-two（on TypeTrigger insert into WinTwo(col2) select col1 from WinOne）、s0 级联（on OtherStream select col2 from WinTwo）——Esper 的 preemptive on-trigger 语义要求路由出的 OtherStream 事件在当前事件的全部语句处理完后才派发，Go 派发循环的 routedQueue 天然满足（queueStatementRoutesLocked 入队、循环结束后统一处理），s0 因此看到 WinTwo 已写入的 col2=9。为此新增两个能力：(1) select 触发器路由——TriggerQuery.Query 携带 routeTarget、validateRoute 放行 triggerSelectTable（其余触发动作仍拒绝），镜像 Esper insert-into 流目标的统一形态，Go 写法为 OnRecord(FromAny(...)).SelectFromNamedWindow(...).Query(RouteTo(...))；(2) InsertIntoNamedWindowFrom(target, source, assignments) 新触发动作（triggerInsertFromNamedWindow），按源窗口每个保留事件投影一行插入目标窗口，NamedWindowField 读源行（绕过 ordered-assignment helper——它会把 Group 换成目标工作行从而遮蔽源行）。Java 的 OtherStream 由 insert-into select 列隐式建型，Go 按既有显式 schema 约定注册 map schema。新增 1 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；bean-backed/schema-backed/contained/deep-supertype、intersection 与 select-stream-dot-star 仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1533/4140 (37.03%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 51/58 execution：落地命名窗口 pattern 消费对照，并修正 Go pattern 引擎的 or-quit 与单发语义。InfraPattern 对照 every a=MyWindowPAT(key='S1') or a=MyWindowPAT(key='S2')——Esper 把 every 只解析到第一分支，S1 两次重复触发（isQuitted=false），S2 单次完成时 EvalOrStateNode 以 isQuitted=true 退出整个 or 表达式（含 every 兄弟分支），之后的 S1 保持静默。runtime.go 新增 patternCompletionPermanent 与 patternProgress.quit 标记镜像该传播：or 在某分支永久完成时置 quit 并使兄弟分支不再产生新完成，quit 状态对父节点保持已捕获真值；同一修正让无 every 的根 pattern 单发——普通 filter/sequence/and 完成后语句静默而非每事件重启新匹配（Java 探针实证 plain filter、and、and+@consume、sequence 均只首发一次，且 and+@consume 的首发行为 [E2, E1]），两个 Go 原生断言按 oracle 改写（and consume 测试只保留 {E2,E1} 一行、within-expr 截止时间测试显式加 every 保留重启语义）。新增 1 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；bean-backed/schema-backed/contained/deep-supertype、intersection、select-stream-dot-star 与 on-insert preemptive two-window 仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1532/4140 (37.00%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 50/58 execution：落地多键 groupwin 保留与 grouped late-start 两个对照。GroupWindowSpec 新增 Keys []Expr 多键支持（GroupWindowKeys([]Expr{...}, inner) 构造，effectiveKeys() 在单键时行为不变），runtime.go 抽出 groupWindowKeys 逐键 (state,value) 对经 encodeKey 合成组身份，state.go 的 expelGroupedLengthOverflow 与 expireNamedWindowGroupedBatchRollover 及全部调用点统一走 effectiveKeys。SelectGroupedViewLateStart 对照 #groupwin(theString, intPrimitive)#length(9)——3×3 网格加三个非均匀事件共 12 条填充后，晚部署的 group by theString, intPrimitive count(*) 消费语句按 Esper 语句启动期 preload 语义迭代出 10 个分组 {c0,0,1},{c0,1,2},{c0,2,1},{c1,0,1},{c1,1,1},{c1,2,2},{c2,0,1},{c2,1,1},{c2,2,1},{c3,3,1}（order by theString, intPrimitive）；SelectGroupedViewLateStartVariableIterate 对照变量驱动 having——窗口 schema 用独立的 nwViewsGroupedFull struct 对应 Esper create-window 派生事件类型（同一 struct 以第二个名字注册会重指 Go 类型→事件类型索引，导致 SupportBean 发送错路由，已作为差异记录进 manifest），avg(longPrimitive) as avgLong / count(boolPrimitive) as cntBool 分组聚合，on SupportVariableSetEvent(variableName='var_1_1_1') set 语句写变量，having theString = var_1_1_1 在每次变量设置后把迭代器收窄到对应组（c0 → {c0,0,0.0,1},{c0,1,1.0,1},{c0,2,2.0,1}；c1 → {c1,0,0.0,1},{c1,1,5.5,2},{c1,2,2.0,1}）。新增 2 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；bean-backed/schema-backed/contained/deep-supertype、intersection、pattern、select-stream-dot-star 与 on-insert preemptive two-window 仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1531/4140 (36.98%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 48/58 execution：落地 late-consumer 部署回放、prior 窗口历史与命名窗外连接。对照 Java 语义发现并修复三个运行时缺口——(1) 部署回放：新增 seedNamedWindowConsumerLocked 在 Deploy 期把命名窗口保留内容重放进普通/聚合/连接消费语句的自身状态(丢弃全部批,不触发 listener),对应 Esper 语句启动期的 NamedWindowConsumerPreload;并按 Esper StatementAgentInstanceFactorySelect.handlePreloads 的 isParentBatchWindow 跳过规则,对 time_batch/length_batch/time_length_batch/firsttime/ext_timed_batch/expression_batch 保留的窗口不做回放(namedWindowRetentionIsBatch,groupwin 递归看内层),既有 TimeBatchLateConsumer 的边界整批聚合为 6 语义由此固定。(2) prior 历史：消费语句维护 namedWindowArrival 到达序历史,queryUsesPreviousAccess 门控,prior/prev 表达式按「含自身的到达前缀」求值(Esper prior(N,x) 按既有约定映射为 Go Prior(N-1,x))。(3) 连接快照:snapshotJoinBatch 容忍空 joinState,让命名窗口侧的当前内容在部署后即可见。四个对照——LateConsumer(#uni(value) 为单变量统计派生视图而非 #unique 数据窗口,Go 以 Ungrouped Aggregate + UnivariateStatistics(field).Average() 表达,晚部署迭代器 {1.5}、E3 产生 IR {5/3}/{3/2},count(*) 消费语句同步晚部署);LateConsumerJoin(命名窗口 LEFT OUTER JOIN keepall 市场流,部署即迭代 {E1,1,null},{E2,1,null},S1 到达联接两行,E3 插入反向联接 S2);FilteringConsumerLateStart(过滤 irstream sum 晚部署迭代 {7},过滤外事件删除静默);PriorStats(prior(1,key)/prior(2,key) 逐事件对照 {null,null}→{E1,null}→{E2,E1}→{E3,E2},#uni average 非 irstream 消费同步)。新增 4 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views(status partial);bean-backed、intersection、pattern、grouped late-start/variable-iterate、select-stream-dot-star 与 on-insert preemptive 仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1529/4140 (36.93%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 44/58 execution：落地 TTL 动态过期删除与 invalid 校验矩阵。TimeToLiveDelete 对照 #timetolive(current_timestamp()+longPrimitive)——整 bean 窗口按每事件截止时间动态过期,TTL 表达式以 CurrentTime()+longPrimitive 的 epoch 毫秒 Func2 表达,OnEvent(...).MergeInsertIntoNamedWindow 以永不命中条件 Literal(false)+CopyMatchingFields 对应 Java 的 on SupportBean merge MyWindow insert select * 简写,on-delete 按 p00 中途删除后与时间过期交错对照任意序迭代器(advance 500 无操作、删除 E2/E1、advance 1000 过期 E3、advance 2000 过期 E4);Invalid/InvalidAlreadyExists/InvalidConsumerDataWindow 三个 invalid 对照把 Esper 编译/部署期边界落到 Go 注册/构建期——state.go 新增 groupwin 子视图必须是数据窗口视图的 NewNamedWindowDefinition 校验(#groupwin(value)#uni(value) 拒绝,ErrorInvalidRule),DeleteFromNamedWindow 未声明窗口在 Build 期 ErrorUnknownName,重复窗口名在注册期 ErrorDependency(对应 Esper 部署期 precondition),plan.go 新增 named-window 消费链(穿透 filter 节点)声明数据窗口在 Build 期 ErrorInvalidRule(对应 Esper validateNoDataWindowOnNamedWindow,groupwin 非数据窗口视图故豁免);既有 TestContextPartitionedNamedWindowConsumerRoutesByPartition 按 Java oracle 改写为无窗口消费(分区路由断言保留),EPL 文本专属 invalid 形态(无视图 create window、on-demand update EPL、istream FAF update、model-after 未解析属性)在 Go 链式 API 下不可构造(CreateNamedWindow 要求显式 schema、保留默认 keep-all、update 为 typed 赋值),已登记为差异。新增 4 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views(status partial);bean-backed、intersection、late consumer/join、pattern、prior stats、select-stream-dot-star 与 on-insert preemptive 仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1525/4140 (36.84%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 40/58 execution：落地 on-delete 别名矩阵、双插入与过滤消费者，并修正分组 irstream 聚合的建组语义。WithDeleteUseAs/FirstAs/SecondAs/NoAs 四个 EPL on-delete 别名变体在 Go 链式构造下合并为单一 typed 写法（typed trigger/window 字段使流别名失去必要），共享 tryCreateWindow 消费者矩阵对照：s0 value*2 计算投影、s2 按 key 分组 sum 的 irstream IR pair、s3 value>=10 过滤，以及 no-op delete 三方静默（Go 删除触发语句把被删事件作为触发批的 old data，Esper 则作为 on-delete 语句的 new data，二者都只在命中时触发——Java 的 no-op assertListenerInvoked 实由首次删除的残留 invocation 满足，故按调用次数对照而非触发负载）；DoubleInsertSameWindow 对照两条 insert-into 写同一窗口——各自交付独立窗口 delta（窗口 listener 两次调用），消费语句观察到 {E1,11},{E1,12} 扁平化两行（触发赋值支持 Add 计算列）；FilteringConsumer 对照 unique(key) 窗口 + (value>0, value<10) 过滤 irstream 消费者——过滤器同时作用于 new 与 old 事件，unique 替换中新事件不满足过滤时消费者只收到 old 行，s0 迭代器按过滤后状态断言。runtime.go 修正分组 irstream 聚组建组时补发 Esper null-prior old 行（group-by 列填充、聚合列 null），两个既有 Go 原生测试（aggregate-where/join-aggregate-where）按 Java oracle 语义同步更新断言。新增 6 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；bean-backed、intersection、late consumer、pattern、TTL 与 invalid 矩阵仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1521/4140 (36.74%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 34/58 execution：落地 per-group 保留。LengthWindowPerGroup 对照 #groupwin(value)#length(2)——每组保留最新 2 个事件，组内超限时最老事件与触发插入同 delta 以 old 离窗（IR pair），窗口 listener 与 irstream 消费语句同步断言，迭代器保持插入序，on-delete 中途删除；TimeBatchPerGroup 对照 #groupwin(value)#time_batch(10 sec)——与 time_batch 共用锚定边界调度与静默累积/删除，边界 flush 时按组 key 首见顺序分组拼接交付整批 new data（E1,E4 组 10 先于 E2,E3 组 20）。state.go 新增 GroupWindowSpec 的 named-window 保留支持：插入路径按 inner 分派（length 逐组超限淘汰 expelGroupedLengthOverflow、time_batch 锚定静默累积）、expire 路径新增 expireNamedWindowGroupedBatchRollover 分组 flush、merge 插入路径同步补齐、批量 inner 的未 flush 删除静默。新增 2 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；bean-backed、intersection、filtering/late consumer、pattern、TTL 与 invalid 矩阵仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1515/4140 (36.59%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 32/58 execution：新增八个对照，落地四个外部时间/批量视图。ExtTimeWindow/ExtTimeWindowSceneTwo 落地 ext_timed(value, 10 sec)——窗口随到达事件的外部时间戳滑动，当已见最新时间戳达到事件时间戳+周期时过期，过期 old 与触发插入的 new 同处一个 delta（窗口 listener 与 irstream 消费语句同步收到 IR pair），on-delete 提前移除；ExtTimeWindowSceneThree 虽以 ext_timed 命名但实际覆盖 #time(10) 持有完整 SupportBean 事件 + irstream wildcard 消费 + SupportBean_A.id 对 theString 的 on-delete（复用 TimeWindow 保留路径）；TimeOrderWindow/TimeOrderSceneTwo 落地 time_order——按外部时间戳排序保留（快照按时间戳序）、在引擎时钟下于时间戳+周期过期，外部时间戳已过期的事件直接穿透并与自身组成 IR pair；TimeLengthBatch/TimeLengthBatchSceneTwo 落地 time_length_batch——与 time_batch 共用锚定边界 rollover（state.go 抽出共享 expireNamedWindowBatchRollover），尺寸触发在插入路径、时间触发在引擎时钟路径，先到先触发，静默累积且未 flush 事件的删除静默，全空 rollover 解除调度、下次插入重新锚定（SceneTwo 首次 flush 在 25000 而非原调度的 21000）；ExternallyTimedBatch 落地 ext_timed_batch(value, 10 sec, 0L)——以 epoch 参考点锚定边界静默累积，累积事件的删除正常发 old，到达事件时间戳跨下一边界时将当前批以 new data 交付、上一已完成批以 old data 离窗，新事件开启下一批。state.go 为 ExternallyTimedWindowSpec（含 Batch）/TimeOrderWindowSpec/TimeLengthBatchWindowSpec 新增 named-window 保留支持，merge 插入路径同步补齐。新增 8 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；per-group、bean-backed、intersection、filtering/late consumer、pattern、TTL 与 invalid 矩阵仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1513/4140 (36.55%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 24/58 execution：新增六个对照。LengthBatch/LengthBatchSceneTwo 落地 length_batch 语义——与 time_batch 共用已完成批状态机（batchLast）但按条数触发：静默累积、满 size 整批以 new data 交付、上一已完成批在同一回调以 old data 离窗，未 flush 事件的删除静默；LengthFirstWindow 落地 firstlength——仅放行前 size 个事件，窗口满后插入静默丢弃，删除腾出空位后恢复放行（普通与 merge 插入路径同步补齐）；LengthWindowSceneThree 覆盖 length(2) 持有完整 SupportBean 事件 + irstream wildcard 消费 + SupportBean_A.id 对 theString 的 on-delete；TimeAccum/TimeAccumSceneTwo 落地 time_accum——插入即时交付，全部保留事件共享以最新保留事件为锚的过期时刻、整窗一起作为 old 离窗，删除最新事件后锚点回退到次新事件（state.go 新增 TimeAccumWindowSpec 保留分支）。新增 6 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；ext_timed/timeorder/timelengthbatch/external-timed、per-group、bean-backed、intersection、filtering/late consumer、pattern、TTL 与 invalid 矩阵仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1505/4140 (36.35%). All gates pass.

本轮扩展 InfraNamedWindowViews 切片至 18/58 execution：新增六个时间视图对照（全部跑在 WithStartTime 虚拟时钟 + Engine.AdvanceTime 上）。TimeWindow/TimeWindowSceneTwo 覆盖 #time(10 sec) 滑动过期——事件恰在插入时间+周期边界过期，窗口 listener 与 irstream 消费语句同步收到 old，on-delete 提前移除；TimeFirstWindow 落地 firsttime 语义（state.go 新增 FirstTimeWindowSpec 保留支持）：窗口以最早保留事件为锚，仅在锚+周期之前放行新事件，之后插入静默丢弃且无 listener 回调，定时器永不主动过期（普通插入与 merge 插入路径同步补齐）；TimeBatch/TimeBatchSceneTwo/TimeBatchLateConsumer 落地 time_batch 状态机（namedWindowRuntime 新增 timeBatchBoundary/timeBatchLast）：当前批静默累积（插入与未 flush 事件的删除均不触发 listener），锚定在首次插入的边界时刻整批以 new data 交付、下一边界再以 old data 离窗（同一边界两批并存时单次回调同时携带 new 与 old），边界调度跨空批按周期滑动，LateConsumer 验证窗口开始累积后部署的 sum(value) 聚合消费语句在边界收到整批并聚合为 6。新增 6 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；ext_timed/timeorder/timeaccum/timelengthbatch/external-timed、lengthbatch/firstlength、per-group 等仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过。Coverage 1499/4140 (36.21%). All gates pass.

本轮开启 InfraNamedWindowViews 切片（12/58 execution）：新增 infra_namedwindow_views_parity_test.go 十二个对照，覆盖 named window 数据窗口保留矩阵——KeepAllSimple（keepall 持有完整 SupportBean 事件，insert-into select * 以 OnEvent+InsertIntoNamedWindow 全列赋值等价表达）、KeepAllSceneTwo（keepall + on-delete 触发器 + 逐步迭代器断言）、LengthWindow/LengthWindowSceneTwo（length(3) 满窗淘汰最老事件的 IR pair 在窗口 listener 与 irstream 消费语句两侧同断言，on-delete 中途删除）、LastEvent/LastEventSceneTwo（lastevent 替换产生 IR pair、on-delete 清空）、FirstEvent（state.go 新增 FirstEventWindowSpec 保留支持：窗口占用后插入被静默丢弃且完全不触发 listener 回调，普通插入路径与 merge 插入路径同步补齐，对应 Esper firstevent 语义）、Unique/UniqueSceneTwo（unique(key) 重复键替换 IR pair，快照按首见键序对应 Java any-order 断言）、FirstUnique（firstunique(key) 重复键静默丢弃）、SortWindow/SortWindowSceneTwo（sort(3, value asc) 迭代器按排序序返回，满窗淘汰最高值 IR pair，同值 tie 时新事件排在旧事件前——reverseSortedEqualRuns，排序超出保留大小的插入在同一 delta 内自增自删产生与自身的 IR pair）。窗口迭代器状态以 NamedWindow.Snapshot 按保留序读取对应 Java assertPropsPerRowIterator。新增 12 个 Java runtime 已登记到 case.infra-namedwindow-views 与 infra.namedwindow.views（status partial）；timewindow/exttime/timeorder/timeaccum/timebatch/timelengthbatch 时间视图、lengthbatch/firstlength、per-group、bean-backed/contained、intersection、filtering consumer、late consumer、pattern、TTL 与 invalid 矩阵仍为后续切片。Java TestSuiteInfraNamedWindow 17/17 通过（含 InfraNamedWindowViews 58 executions）。Coverage 1493/4140 (36.06%). All gates pass.

本轮关闭 EPLSubselectUnfiltered 切片（19/19 execution，18 个 Go 对照）：新增 epl_subselect_unfiltered_parity_test.go 覆盖无过滤标量子查询全矩阵——UnlimitedStream/LengthWindow（tryAssertMultiRowUnfiltered 多行窗口场景，标量子查询多行命中按 SubqueryNullOnMultiple 返回 null）、AsAfterSubselect/WithAsWithinSubselect/NoAs（As 命名在子查询外/内/缺失三种形态）、LastEvent（lastevent 窗口单行场景 tryAssertSingleRowUnfiltered）、Expression（子查询投影为字符串 concat）、TwoSubqSelect（同 select 两个计算子查询）、WhereClauseReturningTrue/WhereClauseWithExpression（子查询内 where 常量真/表达式谓词）、ComputedResult（对标量子查询结果做外层算术且 null 穿透传播）、FilterInside（子查询内流过滤器）、CustomFunction（Func1("minusOne") 自定义函数参与子查询谓词）、SelfSubselect（insert-into count 流经 lastevent 子查询回读，只能看到此前已路由事件的计数而非在途触发事件）、StartStopStatement（常量真子查询门控外层流，undeploy/redeploy 重置子查询窗口状态）、JoinUnfiltered（S0×S1 keepall join 内两个无相关无过滤子查询，多行命中返回 null，末轮 3 行累积以 listener.allRows 断言）、StreamPrior（OM/Compile 变体共享运行时语义同测，Esper prior(0, x) 为候选行自身按约定映射为行字段、prior(N, x) 映射为 Go Prior(N-1, x)）。plan.go 的 validateSubquery 新增两项 Esper 校验：标量投影内嵌套子查询拒绝（subquery-within-subquery；Go 保留 SubqueryExists 谓词嵌套与 SubqueryRow 多列 fragment 两个已登记扩展，故仅收窄到 scalar projection 形态）与子查询 filter 内聚合函数拒绝（aggregation functions are not supported within subquery filters）；InvalidSubselect 对照断言 windowless select/filter、invalid property、nested、aggregate-in-filter、non-bool predicate 六类 Build 拒绝，unqualified-property 与 IN-wildcard-type-mismatch 在 Go 类型化 builder 下编译期不可能（approved difference），group-by/order-by 内子查询拒绝仍为后续切片。新增 19 个 Java runtime 已登记到 case.epl-subselect-unfiltered 与 epl.subselect.unfiltered（status partial）。Java TestSuiteEPLSubselect 19/19 通过。Coverage 1481/4140 (35.77%). All gates pass.

本轮关闭 EPLSubselectFiltered 切片（累计 26/27 execution，新增 12 个对照）：多流外层 join 相关（Joined2Streams/3Streams/3SceneTwo 以 JoinField(source, name) 引用外层 join tuple 的 s1.p10/s2.p20/s3.p30；Joined4Coercion/4BackCoercion 覆盖 int/long/double 属性跨流数值 coercion，EqualOf 混合类型相等，三种与两种 AND 操作数排序变体同测）；JoinFilteredOne/Two 覆盖 join where 中的标量子查询（p00||p10 concat 与 EqualOf 比较、where 整体为布尔投影子查询）与 select 中的 prior/prev 子查询（Esper prior(1, x) 按既有约定映射为 Go Prior(0, x)，prev(1, x) 按窗口历史）；MultikeyWArrayPrimitive/2Field/Composite 以 Is 的 null 安全深比较表达 int 数组内容相关键（null 数组匹配 null 数组、多行命中返回 null，SubqueryNullOnMultiple）；SubselectMixMax 以 SortWindow(1, Descending/Ascending(measurement)) 承载 sort 窗口 wildcard 子查询返回最高/最低事件负载；SubselectPrior 落地 insert-into 链对照——Join 两路 device 过滤 lastevent 流按 type 相等后 SelectLeft/SelectRight 以 JoinEventValue 插入 Pair map 流，消费端 FromAny("Pair") 过滤器以 Coalesce(SubqueryValue(pdrLast), -1) 对比目标流 lastevent 子查询去重（Esper wildcard join fragment 属性 a.id 由新增的 NestedField(Event, name) 表达式导航插入的 Event 值）。新增 expr.go 的 NestedField 能力并登记 12 个 Java runtime 到 case.epl-subselect-filtered 与 epl.subselect.filtered；套件仅剩 SelectWildcardNoName（subselect_1 自动命名为 EPL 语法糖，Go 要求显式别名，approved difference）。Java TestSuiteEPLSubselect 19/19 通过。Coverage 1462/4140 (35.31%). All gates pass.

本轮开启 EPLSubselectFiltered 切片（14/27 execution）：修正子查询 having 语义——Esper 允许非聚合、无 group-by 的 having，在 where 之后逐行过滤（subquery.go 非聚合候选路径补 having 求值，plan.go 移除「having 需要聚合投影或 group-by」的校验拒绝，原拒绝断言改写为接受断言）；新增 epl_subselect_filtered_parity_test.go 十个对照：HavingNoAggNoFilterNoWhere/WWhere/WFilterWWhere（having 逐行过滤、与 where/流过滤器组合）、SelectWithWhereJoined（OuterField 相关标量子查询 p10=s0.p00）、WhereConstant（单列常量、双列常量、between 范围三场景，多行命中时用 SubqueryNullOnMultiple 对应 Esper 标量子查询多行返回 null）、WherePrevious（子查询投影内 Prev(1,id) 按窗口历史求值，OM/Compile 变体与 EPL 运行时语义一致同测覆盖）、SameEvent（同流 wildcard 标量子查询返回触发事件本身，Compile/OM 变体同测覆盖）、SelectWildcard（异流 wildcard 返回最新内流事件负载）、SelectWithWhere2Subquery（外层 where 中两个标量子查询 OR 组合）、SceneOne（irstream + 相关子查询，length(2) 滑出时 old 行保留原始计算值）。新增 14 个 Java runtime 已登记到 case.epl-subselect-filtered 与 epl.subselect.filtered（status partial）。Joined2/3/4Streams 多流相关与数值 coercion、JoinFilteredOne/Two、SubselectMixMax（sort 窗口）、SubselectPrior（insert-into 链 + coalesce）、MultikeyWArray 数组相关键仍为后续切片；SelectWildcardNoName 的 subselect_1 自动命名为 EPL 语法糖（Go 要求显式别名，approved difference）。Java TestSuiteEPLSubselect 19/19 通过。Coverage 1450/4140 (35.02%). All gates pass.

本轮扩展 EPLOtherDistinct 切片至 16/20 execution：新增四个对照——OnSelectMultikeyWArray（on-trigger SelectFromNamedWindow + 数组 key distinct；修正 trigger.go 在 trigger select 结果路径补 distinctSnapshotResults 去重）、VariantStream（RegisterVariant + InsertInto 路由事件至 variant 流 + 三条 distinct 查询覆盖 wildcard/single-array/two-array）、Subquery（SubqueryIn + AsRecord 表达 IN 子查询中的 distinct，验证重复 id 不影响 IN 匹配语义）、OnDemandAndOnSelect（CreateNamedWindow + InsertNamedWindow 填充 + ExecuteFireAndForget distinct + order by 对照 Java FAF 矩阵）。新增 4 个 Java runtime 已登记到 case.epl-other-distinct 与 epl.other.distinct。BatchWindowJoin（length-batch join delta distinct）、JoinPatternOne/Two（every-distinct pattern + unidirectional join）仍为后续切片；EPLOtherBeanEventWildcardSODA 为 approved-difference。Coverage 1436/4140 (34.69%). All gates pass。

本轮开启 EPLOtherDistinct 切片（12/20 execution）：修正 distinct 核心语义——applyDistinct 对 new 结果改为「单次窗口更新内首次出现」去重（连续视图 keepall 每更新单事件故每事件重发；批处理视图 length_batch 整批一次更新故批内去重），同时全局引用计数跟踪每个 key 共享的事件数，old 流仅在最后一个共享事件离开时触发；snapshotBatch 与各 output 限速 flush 路径（OutputEvery 计数、OutputEveryTime 时间、snapshot）补 distinctSnapshotResults 去重；wildcard（select *）结果经 resultEvent 承载，snapshot helper 同时读 row/event 两种 Result。新增 epl_other_distinct_parity_test.go 十一个对照 + 一个 FAF 对照：OutputSimpleColumn（单流+join keepall 逐事件重发）、BatchWindow/BatchWindowInsertInto（length_batch 批内去重 + insert-into 跨 listener 调用累积，对应 Java getNewDataListFlattened）、BeanEventWildcardThisProperty/PlusCols/MapEventWildcard（wildcard distinct）、OutputLimitEveryColumn/RateSnapshotColumn（output 限速 flush 去重）、OutputLimitMultikeyWArray 单/双数组（OutputEveryTime 时间 flush + 数组 key encodeKey）、IterateMultikeyWArray（snapshot 数组 key）、FireAndForgetMultikeyWArray（FAF named-window 数组 key distinct）。distinctListener 改为累积式以贴合 Java getNewDataListFlattened 语义。新增 12 个 Java runtime 已登记到 case.epl-other-distinct 与 epl.other.distinct（status partial）。BatchWindowJoin（length-batch join delta distinct）、OnDemandAndOnSelect、Subquery、OnSelectMultikeyWArray、VariantStream、JoinPatternOne/Two（every-distinct pattern + unidirectional join）仍为后续切片；EPLOtherBeanEventWildcardSODA 为 approved-difference（Go 用不可变 fluent Plan 而非 SODA 文本/模型解析器）。


本轮关闭 EPLFromClauseMethodWConfig 五个类 17 个 execution：新增 fromclausemethod_cache_parity_test.go、fromclausemethod_multikey_parity_test.go、fromclausemethod_variable_parity_test.go 与 fromclausemethod_joinperf_parity_test.go 四个对照文件。CacheLRU/CacheExpiry 用 CachedMethodProvider（MethodCacheConfig LRUSize=3 / MaxAge=1s+PurgeInterval=10s）包装可计数 provider，逐事件断言 LRU 淘汰与过期命中调用次数；MultikeyWArray 以 Func2+slices.Equal 表达数组字段相等 where 谓词，并以数组内容/null 数组为缓存 key（encodeKey 的 %T:%#v 天然区分 []int 与 nil）；Variable 用 RegisterVariable/RegisterContextVariable 覆盖常量/非常量/context 分区变量服务，on-set 语句写 postfix，ContextVariable 以 CreateOverlappingPatternInitiatedTerminatedContext + ContextPatternField 按分区隔离 postfix，VariableMapAndOA 覆盖变量驱动 map/OA 方法 handler 的 iterator-only 快照；JoinPerformance 四个 correctness port（grid 与轮次缩减，Go 无 join on-condition index 故 cross-product 评估，timing 断言按既有 perf parity 惯例略）。为此重构 updateJoin 无依赖方法侧语义：方法行改为只 poll 新接受的 trigger 行并按 lineage 保留、随 trigger 行级联过期（removeStoredEventsCascade），取代原先每周期清空+全量重 poll，使调用次数与缓存命中贴合 Java 每事件一次 poll 的语义；同时区分混合语句（有事件驱动源）与纯方法语句（无事件驱动源）——前者在无 trigger 接受的周期（无关类型事件或被 filter 拒绝）不再 poll 方法侧，避免幽灵行；pattern-initiated overlapping context 的分区 key 改由 initiating 事件身份 + 语句内序号派生，使跨语句 context 变量共享一致。EPLFromClauseMethodVariableInvalid 保持 approved-difference：footprint/null-metadata 反射诊断在链式 API 无对应，context 变量作用域校验由 TestFromClauseMethodContextVariableParity 与 TestContextVariableScopeValidation 覆盖。新增 17 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethodWConfig 5/5、TestSuiteEPLFromClauseMethod 3/3 通过；unidirectional-historical、完整 Java 诊断文本与 join on-condition index 优化仍为后续切片；本轮不需要 MySQL Docker。

本轮关闭 EPLFromClauseMethod 主类全部 20 个 execution：新增 fromclausemethod_eventbean_parity_test.go 六个对照，覆盖 2JoinEventItselfProvidesMethod（事件类自身静态方法提供的 [10:20] 区间界——EvaluateOnce lower/upper 种子行驱动 join-where 过滤，9/21 静默、10/20 触发）、StreamNameWContext 与 WithMethodResultParam（provider 经 MethodInvocationContext 观察语句名，嵌套方法结果参数折叠进 provider，产出 somevalue/E1/s0 行）、EventBeanArray（Java EventBean[]/Collection/Iterator 三种返回形态统一为 Go []Event provider 契约，事件类型由 provider schema 声明，@type 注解及其限制诊断无 Go 对应，已登记差异）、InvocationTargetEx（provider 错误直接作为 SendEvent 错误返回，对应 Java 重抛的调用目标异常）与 UDFAndScriptReturningEvents（UDF/JS 脚本事件生产者统一折叠进 provider，JVM JS 引擎宿主保持 Java 专属并登记差异）。至此 case.epl-fromclausemethod-basic 由 partial 转为 mapped（主类 20 + NStream + OuterNStream 全部登记）；EPLFromClauseMethodCacheLRU/CacheExpiry/JoinPerformance/MultikeyWArray/Variable 五个类、context 绑定、unidirectional-historical、多事件流无依赖方法绑定与完整 Java 诊断仍为后续切片。新增 6 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethod 3/3 通过；本轮不需要 MySQL Docker。

本轮补齐方法源 with-arg 四切片：对照 Java EPLFromClauseMethod 主类的 ArrayWithArg/ObjectWithArg/2StreamMaxAggregation/NoJoinIterateVariables，新增 fromclausemethod_witharg_parity_test.go 四个对照。ArrayWithArg 以 fetchArrayGen(intPrimitive) 覆盖 method-first/declared 两文本顺序，逐事件断言 irstream——E3 只产生 {A,E3}，E6/E7 到达时过期事件携带各自轮询行离开（old {A,E3}、{A,E4},{B,E4}）；ObjectWithArg 用空串建模 Java null theString，fetchObject 单行/空结果对照（SODA 文本等价变体按惯例跳过并登记）。为此修正 join 运行时：事件驱动 join 中无依赖方法侧按接受触发的流的保留行逐行重求值（joinCycleOrder 让事件侧先于查找侧，updateJoin 内轮询行携带 trigger 行 lineage），轮询行只在所属 trigger 元组内成组并随其过期消失，贴合 Esper 每事件轮询由 join 窗口保留的语义；无触发单方法源语句的 Snapshot 现在用当前变量重轮询（currentStreamEvents），NoJoinIterateVariables 的 {5..10}/null/{4} 迭代器矩阵与 listener 全程静默由此固定；2StreamMaxAggregation 覆盖 unique(theString) × 100 行网格的 max(col1)={9}（重复键事件按 Java assertPropsPerRowLastNew 等价建模）。新增 4 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethod 3/3 通过；EventBeanArray、UDF/script、StreamNameWContext、WithMethodResultParam、InvocationTargetEx、context 绑定、multikey/array-key、cache、unidirectional-historical、多事件流无依赖方法绑定与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

## 2026-08-08

本轮补齐方法源 2JoinHistorical 五兄弟切片：对照 Java EPLFromClauseMethod 主类的 2JoinHistoricalIndependentOuter/SubordinateOuter/SubordinateOuterMultiField/OnlyDependent/OnlyIndependent，新增 fromclausemethod_historical_join_parity_test.go 五个对照。IndependentOuter 以两个 EvaluateOnce 常量方法源（fetchResult12/fetchResult23 值网格）覆盖 LEFT/RIGHT/FULL/FULL-reversed 四文本变体的部署即迭代器状态 {1,null},{2,2} 与 FULL 追加 {null,3}；SubordinateOuter 让 s1（fetchResult23(s0.value)）经 DependingOn("s0") 绑定，部署期种子从 EvaluateOnce 侧按拓扑序级联到无触发 subordinate 方法源（seedEvaluateOnceJoinSidesLocked 第二轮），依赖行携带 lineage 永不锚定占位元组，四变体同为 {1,null},{2,2}；SubordinateOuterMultiField 用 keep-all 事件流 LEFT OUTER JOIN 100 行常量网格的双字段等值条件对照 lastNew 与迭代器。OnlyDependent/OnlyIndependent 以 RegisterVariable(name, nil) 对应 Java null 初始化 int 变量，on-set 事件写入 lower/upper 后重轮询 fetchBetween/fetchBetweenString/fetchIdDelimited，并固定 Java 语义：无触发纯方法语句（queryIteratorOnlyMethodSources）只做状态刷新、永不投递 listener/route 输出（含 undeploy 后），SQL-historical 源保持 Go 原生任意事件轮询投递行为不变。新增 5 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethod 3/3 通过；ArrayWithArg/ObjectWithArg、2StreamMaxAggregation、NoJoinIterateVariables、EventBeanArray、UDF/script、context 绑定、multikey/array-key、cache、performance 与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

本轮补齐方法源 NW-twice 切片：对照 Java EPLFromClauseMethod3Stream1HistStreamNWTwice，新增 Stream.As(alias) 链式别名——同一个 keepall Named Window 在单条 Join 中以 us/them 两个逻辑名出现两次，方法源通过 DependingOn("us","them") 按别名做依赖拓扑排序与 invocation 笛卡尔展开，request.Dependency 按别名读取两侧窗口事件；窗口目录查找与事件匹配仍使用注册名（runtime 的 methodJoinEvaluationOrder 与 methodDependencyInvocations 统一走 logicalName）。Go 侧用 InsertNamedWindow 替代 Java 的 insert-into 语句填充窗口，SelectSourceEvent 投影 us/them 完整事件并对照 Java 的两行 {one,two,1}/{two,one,1} 结果。新增 1 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethod 3/3 通过；outer joins、cache、context、multikey/array-key、performance 与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

本轮补齐方法源 OuterNStream 切片：对照 Java EPLFromClauseMethodOuterNStream，新增 Stream.EvaluateOnce() 链式标记——常量参数方法源在 Deploy 时按 Esper statement-start 语义单次求值（seedEvaluateOnceJoinSidesLocked），像只读数据窗口一样保留且不发 listener 批次，触发周期不再清理或重查该侧；validateNode 拒绝 EvaluateOnce 与 trigger/DependingOn 组合。修正 outer join 占位语义贴合 Esper N-way 装配：依赖绑定（subordinate）方法行不再锚定占位元组（joinStoredTupleHasAnchor，joinChainedTuples/joinOuterTuples 的 outer 保留分支统一跳过），后续 outer 边无法求值时（累积元组中不含该边条件引用的真实事件）部分元组原样透传（joinChainedEdgeAnchored）。新增 fromclausemethod_outer_parity_test.go 五个对照：1Stream2HistStarSubordinateInner（s0-first/h0-first 两变体）、1Stream2HistStarSubordinateLeftRight（LEFT×2、h1-first RIGHT+LEFT、h0-first RIGHT+LEFT、FULL×2 四变体，逐事件断言 lastNew 与迭代器全量）、2Stream1HistStarSubordinateLeftRight（LEFT+RIGHT、LEFT×2、RIGHT×2 三变体，fetchVal(s0.id||'H0',s0.p00) 依赖读取）、1Stream2HistStarNoSubordinateLeftRight（EvaluateOnce 部署即 4 行占位、占位随匹配蒸发；Java 第二文本变体 h1 LEFT s0 RIGHT h0 在 Java 断言同一矩阵，按等价 s0 锚定 RIGHT×2 链建模并在 manifest difference 登记）、Invalid（own-outer-join-descendant 与 optional-dependency 两种无法满足的 outer 依赖诊断）。新增 4 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod；outer forward/3-hist subordinate 精确矩阵、cache、context、multikey/array-key、performance 与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

本轮关闭 OuterNStream 剩余三个精确矩阵：对照 tryAssertionThree/Four/Five 补齐 #lastevent 锚定的 outer forward subordinate——1Stream2HistForwardSubordinate（LEFT×2 链，fetchVal('H0',p00)/fetchVal('H1',p01) 常量前缀+依赖行计数 provider）、1Stream3HistForwardSubordinate（LEFT×3 链与 h0-first RIGHT+LEFT+FULL 混合变体均按声明建模，FULL 边上依赖绑定行不锚定占位、无法求值的边透传部分元组，两个变体共享同一事件矩阵）、1Stream3HistForwardSubordinateChain（fetchVal(s0.id||'-H0')/fetchVal(h0.val||'-H1')/fetchVal(h1.val||'-H2') 链式前缀，lastevent 下 trigger 即当前 s0 行）。三个 runtime（b2283f54/51307ad5/9cc6163d）此前已随 NStream 同名类登记，本轮补齐精确对照后 epl.fromclausemethod 的 OuterNStream 覆盖完整；cache、variable/context 绑定、multikey/array-key、performance 与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

## 历史切片记录（自 README 迁入，按原顺序）

本轮 Context Table 与子查询索引补充：相同 primary key 可在不同 Context partition 独立存在；live trigger、分区内 select/update/delete、Context FAF 与 aggregate `IntoTable` scoped replacement 均有对照测试，temporal/initiated partition 释放会回收 scoped Table state 与 ownership。普通全局 Table 的 Context FAF subquery 继续走 indexed candidate；Context-scoped Table 的相关子查询已支持当前 partition 的 primary/secondary hash 与 equality-prefix + B-tree candidate，普通 FAF/Join 会合并 root 与已物化 scoped state 的 candidate；root 中存在 legacy row 时保留 ownership-aware snapshot fallback。Named Window 的 equality/hash 与 equality-prefix + range B-tree subquery shared-index 子集已支持自动创建、显式使用、consumer disable/no-index、Context partition inheritance、multiple-index hint、结构化 index choice、multikey/array key、update/delete rebuild 和 live listener/late-start 对照，并补充 Build invalid 边界。跨 statement transaction、完整 routed/listener rollback、JVM query-plan hook、Esper 完整 cost/selectivity 与性能阈值仍待后续切片。

本轮结果集切片补充了索引化 `First`/`Last`/`Nth`、`WindowEvents`/`EventValue`、`MinByEver`/`MaxByEver`、带过滤器的有状态 `Leaving`、常量/过滤速率与 `RateByTimestamp`/数量速率、多条件 `SortedEvents`、外层分组可嵌套的 `LocalGroupBy`、`SortedAccessBy` 导航访问、无参数 `Method`/属性链、命名及环境注册式 `PluginAggregate`、按组隔离的 `AggregatePluginFactory`（含 `Enter`/`Leave`/`Value`/`Clear`）、context-partitioned rollup/FAF 聚合、确定性 `CountMinSketchAdd`、`TableSink` 聚合落表、链式 `AggregateStream.IntoTable` 原子快照同步、live table snapshot join，以及聚合投影别名 `ResultField` 的 order-by/having 语义；对应 Java 来源和 runtime ID 已登记在 `compat/capability-manifest.json`。这些能力仍是 mapped/partial 切片，不能解读为 Esper 聚合全量对等。

EsperIO 已开始落地 Go 垂直切片：`connectors/csv` 提供 CSV/File source、unformatted line source、CSV sink、Java 风格标题行/类型转换、loop/reset、固定速率/时间戳回放、Esper Event/Row bridge；`connectors/db` 提供 database/sql DML/Upsert sink、绑定、重试、prepared statement 生命周期、same-thread/命名固定 worker 异步 executor、Task 错误观察、优雅 drain 和 MySQL round-trip；`connectors/http` 提供 HTTP client/server source/sink、query/property URI 模板、JSON body、响应上限、重试、暂停/重启和 Engine bridge；`connectors/socket` 提供 TCP source、OBJECT/CSV/PROPERTY_ORDERED_CSV/JSON、Java escape、多连接、暂停/重启、typed Engine bridge 和可注册 object decoder；`connectors/kafka` 提供 kafka-go Reader/Writer、JSON processor、commit/retry/timestamp、key/header/有序 JSON 输出和 Engine bridge；`connectors/amqp` 提供 RabbitMQ Consumer/Publisher、queue/exchange/binding、prefetch、JSON/GOB codec、auto/manual ack、reject/requeue、retry 和 Engine bridge；`connectors/jms` 提供 provider-neutral Spring JMS bridge contract、Map/Text/Object/Bytes codec、Java event-type property、ack-after-process 和 channel transport；七者都使用统一 `OPENED/STARTED/PAUSED/DESTROYED` 生命周期。Java `esperio-csv` Maven 模块 80/80 通过，`esperio-db` 的 2/4 环境结果、`esperio-http` 的 3/4 结果、`esperio-socket` 的 7/7 结果，以及固定 Kafka 3.8.1 KRaft 环境下 `esperio-kafka` 的 6/6 结果已登记到 capability manifest；Spring JMS 4 个测试中 1/4 通过且环境差异已记录，Go Kafka/RabbitMQ/JMS bridge 也通过。Kafka 高级 group/rebalance/plugin、AMQP Java serialization/高级重连、JVM JMS provider/session/transaction/Spring XML 和完整 Dataflow connector 集成仍在计划中。

SQL FAF 的 `EPLDatabaseFAF` 10 个 Java execution 已有 `database_faf_parity_test.go` 对照用例，覆盖多行快照、column/whole-row hook、prepared reuse/close、typed substitution、distinct/where、变量、fluent Plan、SQL 文本参数子查询和 invalid/closed query；`SQLHistoricalProviderOptions.ParameterTypes` 声明 opaque SQL 参数类型，`RowConverter` 支持整行物化与 nil-row skip。fake `database/sql` driver 单元测试不替代上面的真实 MySQL 门禁。

本轮新增基础五字段及可选秒/毫秒 `CronSchedule`/`OutputAt` 与 Pattern `TimerCron`：支持 `CronEvery`、`CronRange`、`CronWildcard`、变量/参数字段、虚拟时钟下一次触发和日月/周 OR 语义；不等于 Esper 全部 cron 能力。

本轮继续补充 JSON provided-underlying 的 Go-native 泛型结构体适配：支持嵌套 slice/struct、JSON tag、UUID、`DateOnly`、`time.Time` 和 `big.Rat`，并通过 `Engine.SendJSON`/`RenderJSON` 验证解析、发送和渲染边界；同时覆盖 schema invalid、constructor-style primitive default、null projection route 和 pattern typed-array。JVM 专属的 public-class/default-constructor/non-static-inner-class 诊断记录为 Go 零值结构体模型的 API 差异。

本轮继续补齐 JSON VM-class/laxness 对照：新增 Go-native `URL`/`URI`，覆盖 UUID、时间、URL、URI 的 scalar/一维数组/二维数组/collection、null、shape mismatch 和渲染；覆盖 byte/short/int/long/float/double/BigInteger/BigDecimal 对应 Go 类型的字符串宽松转换及非法值诊断，并验证 malformed/trailing JSON 与 static/dynamic undeclared-field policy。typed 特殊标量的非法输入现在返回错误，不再静默变成 Null；随后又补齐 core dynamic JSON 的 string/number/bool/null/object/array、混合/嵌套数组和 `42.0`/指数数字词法 round-trip。Java class/list adapter 的其它约定、递归 schema/metadata、完整 nested/dynamic arbitrary-precision 格式和共享 parse-write trace 仍未完成。

本轮继续对照 `EventJsonTypingClassParseWrite`：`NewJSONSchemaFor[T]`/`ParseJSON`/`RenderJSON` 现在覆盖 class simple、递归 nested class、对象的一维/二维数组与 collection、基础类型/Character/rune/BigInteger/BigDecimal 列表、枚举样字符串列表，以及 UUID/LocalDate/LocalDateTime/OffsetDateTime/ZonedDateTime/URL/URI 的 scalar/数组/collection。新增用例固定 missing、null、empty、partial、schema 字段顺序、Character 字符串形态和 typed number lexeme；这组 JSON 对照不需要 MySQL Docker。

本轮补充 Java `InfraNWTableFAF` 对照：`infra_nwtable_faf_parity_test.go` 覆盖 Named Window/Table 的 wildcard、filter、distinct、count/sum、group、IN、二流/三流 join 与 join where；`faf_mutation_test.go` 用 Go-native `OnDemand()` 链覆盖 insert/update/delete/delete-all、参数绑定、`InitialNamedWindowField`、Named Window consumer delta，以及 `WithContext` 下 hash/category selector 的分区内 delete/update/delete-all。Named Window 保存稳定的 partition properties，避免更新后的事件字段改变 selector 所属分区。另修正 ungrouped row-for-event 聚合，使每个源事件都返回共享 aggregate 值。Java `TestSuiteInfraNWTable` 26/26 通过。

本轮继续复核 Java `InfraUpdate`：新增 `OnDemand().UpdateAll(...)`，覆盖 Named Window/Table 无 `where` 全量更新、ordered assignment、`InitialNamedWindowField`/`InitialTableField` 和两种存储的 FAF 结果契约；`Table.Update` 允许主键变更并原位 re-key，保持插入顺序，同步 hash/unique/B-tree index，主键或唯一 secondary index 冲突时保持原子不变。新增 `TestOnDemandUpdateAllMatchesInfraUpdate` 与 `TestTableUpdateRekeysPrimaryKeyInPlaceAndPreservesIndexes`。该切片不需要 MySQL Docker。

本轮继续关闭 Context Table 的持久行归属遗漏：Table 行保留稳定内部 identity，Engine 按 table/context/row identity 记录首次 context partition、代表事件和 selector properties；Context FAF 更新分区字段或主键后，后续 update/delete/delete-all 仍按原 partition 执行，真正删除和 `DestroyContext` 清理归属。强化 `TestOnDemandContextPartitionUpdateAndDeleteAllRespectSelector`，覆盖 Table/Named Window、category selector、分区字段变化、primary-key re-key、原分区删除和剩余分区清理。FAF 目标存储 mutation 在中途主键/唯一冲突或 pending routed processing 失败时会回滚 Table/Named Window 目标状态及 Context ownership；完整跨 statement transaction、跨目标 routed side effect、更广 live Context Table mutation 组合和 transaction/concurrency trace 仍未完成；本轮不需要 MySQL Docker。

本轮补齐 Java `ContextKeySegmentedInfra.ContextKeyedSegmentedTable` 的 live Table ownership：`TestContextTableLiveInsertOwnershipMatchesEsper` 用 Go 链式 `InsertIntoTable`、`UpsertIntoTable`、`MergeInsertIntoTable` 覆盖 A/B 分区写入，更新分区字段后仍由原分区 selector 执行 FAF 删除，并验证 live delete 清理 row identity ownership。Java `TestSuiteContext` 17/17 通过；跨 Context 相同主键隔离、aggregate-into-table 与完整 Context Table transaction/trace 仍待后续切片，本轮不需要 MySQL Docker。

本轮新增 `faf_multirow_test.go` 对照 Java `InfraNWTableFAFInsertMultirow`：`OnDemand().InsertRows(InsertValues(...))` 支持 Named Window/Table positional multi-row insert、Named Window 子查询值、1000 行上限、列数诊断和 unique/primary-key 冲突整批回滚；`NamedWindowUniqueIndex` 提供严格唯一约束并区别于 `Unique` retention 的替换语义。Go immutable fluent Plan 对应 Java SODA/model 路径，核心验证不需要启动 MySQL Docker。

本轮新增 `faf_substitution_params_test.go` 对照 Java `InfraNWTableFAFSubstitutionParams` 的 7 个 execution：增加 1-based typed `ParameterAt[T]`/`PositionalParameter[T]` 与 `ExecuteFireAndForgetWithPositionalParameters`、`PreparedQuery.ExecuteWithPositionalParameters`，覆盖 Named Window/Table 的单值、双值、slice-IN、具名参数、重复参数、缺参、额外参数、混用、跳号和类型错误。Go API 保留具名 `Parameter[T]` 的 immutable map 绑定；EPL 保留关键字解析和精确 Java 诊断不属于 Go fluent 契约。该切片不需要 MySQL Docker；FAF 跨 join/subquery/context 的参数组合、index plan、representation、历史/方法源、事务/并发和完整 trace 仍未完成，module resolve 已在后续切片补齐。

本轮继续新增 `faf_subquery_parity_test.go` 对照 Java `InfraNWTableFAFSubquery`：用 Go-native `SubqueryValue`/`SubqueryGroupRows`、`OuterField` 和 `OnDemand()` 覆盖 Named Window/Table scalar、join、insert、未相关及相关 update/delete/select、Context 两窗口/全局窗口、inner where/group-by，并在 FAF 执行边界拒绝 event-stream source、source filter 和 context mismatch。目标行通过显式 `OuterEvent` 传入相关子查询；新增 `faf_subquery_index_parity_test.go`，让 Context FAF 中基于 `OuterField`/参数的完整 equality/IN scalar/exists 子查询对 Named Window/Table 声明索引执行 candidate lookup，覆盖全量/指定 partition selector、Context-scoped Named Window/Table 的 partition-local index lookup、equality-prefix + B-tree range、root legacy row 的 ownership-aware snapshot fallback、index-hit counter，以及 UDF/Null 等不安全形状的快照回退。Java 的 4 个性能/index execution 仍不宣称 timing parity；本轮仍不需要 MySQL Docker。

本轮新增 `faf_resolve_parity_test.go` 对照 Java `InfraNWTableFAFResolve`：`Environment.RegisterModule`/`Module.RegisterNamedWindow`/`Module.RegisterTable` 与 `Module.NamedWindow`/`Module.Table` 提供 Go-native module namespace，覆盖 A/B module 中同名 `MyInfra` 的 Named Window/Table 独立写入、wildcard FAF snapshot、module-bound runtime lookup、Plan hash/canonical 隔离和 unknown-module Build 失败。Go 不解析 EPL `module` 文本，也不复制 Java `ModuleUsesOption` 或 protected/public class-loader visibility；该差异已登记到 `case.query-fire-and-forget-resolve`。本轮不需要 MySQL Docker。

本轮新增 `faf_index_parity_test.go` 对照 Java `InfraNWTableFAFIndex` 的 12 个 runtime：`IndexHash`/`IndexBTree`、`SecondaryIndex`/`UniqueIndex`、`NamedWindowIndex`/`NamedWindowBTreeIndex`、`UseIndex`/`UseIndexOn` 和 `Plan.IndexPlan()` 覆盖单源/Join 的自动选择、IN/equality/range、数组/复合数组/null key、错误 hint 与聚合 where；`NamedWindow.Lookup` 及生命周期测试验证声明索引在更新、删除、淘汰和回滚恢复后不残留。新增物理执行对照后，Table/Named Window 的单源完整 equality/IN（含 prepared named parameter）会真正走 hash candidate lookup；普通内连接及两流 `LeftOuter`/`RightOuter` 的 optional side 也会按完整 equality key 和 B-tree range 执行 Table/Named Window candidate lookup，支持自动/显式 index、复合 equality-prefix、双边 inclusive range、反向操作数、`JoinWhere`、批量 probe 去重、source filter、preserved-side unmatched 行和插入顺序；Context 单源 FAF 复用同一候选路径，Context-scoped Table 会合并 root 与已物化 scoped index state，新增 Context inner Join、两流 `LeftOuter`/`RightOuter` optional-side，以及相邻条件左深 mixed `Inner`/`LeftOuter` chain 的 equality/range candidate，覆盖 Named Window/Table、全量/指定 partition selector 和索引命中计数，并按 context key 二次过滤索引结果；`faf_subquery_index_parity_test.go` 继续覆盖 Context FAF 子查询的 correlated `OuterField`/parameter equality/IN scalar/exists，以及 equality-prefix + B-tree range candidate，覆盖全量/指定 partition selector、Context-scoped Named Window/Table 的 partition-local index lookup、参数化上下界、root legacy fallback 和 UDF fallback。此外，`TestInfraNWTableSubqCorrelIndexSharingParity`/`TestInfraNWTableSubqCorrelIndexSharingContextPartitionParity`/`TestInfraNWTableSubqCorrelIndexSharingBTreeParity`/`TestInfraNWTableSubqCorrelIndexOptionValidationParity`/`TestInfraNWTableSubqCorrelIndexMultipleIndexHintsParity`/`TestInfraNWTableSubqCorrelIndexChoiceParity`/`TestInfraNWTableSubqIndexShareMultikeyArrayParity` 对照 Java `InfraNWTableSubqCorrelIndex` 的 equality/hash 与 equality-prefix + range B-tree shared-index 子集，覆盖自动 shared index、显式 index、consumer disable/no-index、Context inheritance、插入顺序、multiple-index hint、结构化 index choice、显式/自动 multikey-array key、update/delete rebuild 和 Build invalid。`FullOuter`、右保留或非相邻链式 outer、unidirectional、OR、UDF、Null/Missing、复杂动态表达式和超大多流 probe 安全回退。单源 B-tree range 继续覆盖反向 Between、prepared bounds、Null 边界回退以及更新/删除/清空后的索引维护。Go 不复制 EPL `@Hint` 与 JVM backing-class hook，Context right/full-preserving/non-adjacent chain/unidirectional Join、Esper 完整 cost/selectivity、JVM query-plan hook 和性能阈值继续保持 partial。本轮不需要 MySQL Docker。

本轮又补充 Context FAF Join 的相邻条件左深 mixed chain candidate：仅对 `LeftOuter`/`Inner` 边、且每条边只引用紧邻前一源和新源的链启用 equality/hash 或 equality-prefix + B-tree range 候选；`TestInfraFAFContextChainedMixedOuterIndexCandidateParity` 覆盖 Named Window/Table、`LeftOuter -> Inner`/`LeftOuter -> LeftOuter`、全量/指定 partition、缺失中间源、稳定顺序和 middle/tail 命中计数。RightOuter/FullOuter、非相邻链、unidirectional 与无法安全提取的条件仍回退 snapshot；本轮不需要 MySQL Docker。

本轮补充真正的 B-tree cursor：`lookupRangeMany` 先按 equality prefix 与 lower/upper boundary 做二分区间定位，再在候选区间内保留最终 `indexRangeEntryMatches` 校验；不可比较的 Null/Missing/异构值自动回退完整有序扫描。`TestIndexRangeCursorBoundsAndFallback` 与既有 Table/Named Window range、复合 prefix、更新删除和 Context Join 对照共同固定结果与回退语义；本轮不需要 MySQL Docker。

本轮继续补齐 `EventJsonTypingCoreParse/CoreWrite`：`json_core_parity_test.go` 直接覆盖基本 scalar、Character 多字符取首 code point、wrapper/primitive 一维与二维数组、enum、`big.Int`/`big.Rat`、`Object`/`Object[]`/`Map` 容器，以及嵌套 BigDecimal 对象和数组的解析/渲染矩阵；`RenderJSON` 对 Character 恢复单字符 JSON string。Core JSON 定向测试通过，Java `TestSuiteEventJson` 13/13 仍通过；递归 schema/metadata、完整 dynamic strict-lax、所有 nested/dynamic arbitrary-precision 格式规则和共享 Java/Go trace 仍保持 partial。本轮不需要 MySQL Docker。

本轮再次复核 JSON 遗漏入口：新增 `Engine.JSONSender`/`JSONEventSender` 的解析、发送和路由链、同名异构 schema 的 identity 防串型、`Schema.Getter` 的 schema-bound mapped getter、特殊字段名与 JSON/Map nested-schema 校验，以及显式泛型 `NewJSONFieldAdapter`/`NewNamedJSONFieldAdapter`/`WithJSONFieldAdapter` 的 string↔typed 双向转换、null 保留、类型/输入错误诊断和 Plan identity；同时覆盖 DocSamples 的 Book/Cake nested、dynamic empty、application class 和显式 Environment catalog boundary。sender dispatch 保留 JSON 原始字面量，Java `EventSenderJson`/`EventPropertyGetter`/annotation class-loader/module visibility 则以 Go 显式对象、函数注册和 catalog 隔离替代；完整 fragment/metadata 与 Java module/path 语义仍未完成。本轮不需要 MySQL Docker。

本轮继续补齐 typed JSON 的嵌套 fragment 元数据：struct-valued scalar/indexed 字段会自动生成稳定的 `NestedSchema`，递归指针复用有限 schema 图；新增 `NestedSchemaNames`/`FragmentSchema`、深层 schema-bound getter、`Event.GetFragment`/`GetFragments` 和 null-versus-empty nested-array 渲染对照，显式 `WithNestedPropertySchema` 仍优先。新增 `WithNestedPropertySchemaFrom`，可从显式 `Environment` catalog 解析独立注册的命名 JSON/Map fragment，并覆盖 Java `Friend[]`/`User[]` create-schema 形态、缺失/跨环境诊断和 JSON→Map fragment。该切片对照 Java `EventJsonTypingNestedRecursive`、`EventJsonTypingClassWArrayAndColl` 和 `EventJsonProvidedClass*WithCreateSchema` 入口，但完整 catalog 生命周期、全表示 fragment metadata、模块可见性和共享 parse-write trace 仍保持未完成。

本轮新增 `enum_data_sources_parity_test.go`：用 Go 链式 API 对照 `ExprEnumDataSources` 的 Prev/Sorted View、Named Window/Subquery、部署参数/变量 late binding、nested property schema、method-backed enum object、mapped property、collection cast、sorted access、generic component、access aggregate 和 Table source，新增 15 个 Java runtime 对账；join/pattern、match-recognize/context property、Java Optional 精确 generic/serde 与完整 invalid/type metadata 仍未完成。

示例位于 `examples/stage1`。当前仍是增量移植，不能宣称完整 Esper 对等：枚举/集合方法的完整 Java 类型与无效规则校验、BigDecimal/任意集合类型、复杂嵌套/子查询/访问聚合组合（当前仅有 Named Window/Table 基础子查询）、ROLLUP/CUBE/GROUPING SETS 的组合 grouping specification/Join/Context/FAF/output 全矩阵，Variant 的单列 method-backed member conversion、Java application/type-class metadata、全量动态属性/表示转换/FAF/Dataflow 矩阵，Join/Aggregate/Pattern/Named Window 路由的完整混合 new/old 顺序、事件表示/转换和可配置循环策略，完整多流/子查询 Join，历史 SQL 的方言占位符、metadata-origin/类型映射、连接池/事务/连接生命周期、DML/Upsert、方法流和多源 FAF/Context 约束，完整聚合和输出策略（filtered/math-context/访问聚合、cron 微秒精度、特殊日历运算符、完整 when/then 上下文与更多计数函数、完整 snapshot-after/grouped delivery 等；日历 after 目前只覆盖非负 years/months/days 的固定周期）、Pattern guard/observer timer-schedule/consumption 及 Match Recognize 的复杂 NFA/prev/interval/after/聚合/窗口删除语义、Context selector/嵌套/全生命周期、Dataflow 类型化多端口/信号/背压、完整事件格式与 Serde、EsperIO 其余传输连接器及完整 Dataflow/trace parity、Java/Go 全量行为差分和源测试映射仍按规划继续实现。Java 运行态 execution 清单已生成在 `compat/java-execution-inventory.jsonl`，但它不等于 Go 已通过对照。

本轮继续补齐方法源 N-stream 域：对照 Java EPLFromClauseMethodNStream，新增 FromAs[T] 支持同一事件类型在单条 Join 中以 s0/s1/s2 不同别名多次出现，并对照完成 2-stream + 2 hist 的 star-subordinate、3-stream + 1 hist 的 subordinate 切片。方法源通过 DependingOn("s0") 从 request.Dependencies 读取驱动流事件，fcmMakeSenderMatchFields 按 ids0/ids1/ids2 过滤 keepall 全量重放行，保留 Java assertPropsPerRowLastNew 语义。plan.go 的 validateNode、sourceSchema 对显式 isAlias 节点做条件式 schema 回退，避免无条件别名回退破坏既有 regression。新增 2 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethod 3/3 通过；NW-twice、pure-no-subordinate、outer joins、cache、variable/context、multikey/array-key、performance 与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

本轮继续补齐方法源变量驱动 3-historical 切片：对照 Java EPLFromClauseMethodNStream 的 3HistPureNoSubordinate、3Hist1Subordinate、3Hist2SubordinateChain，新增 FromMethod + RegisterVariable + OnEvent SetVariables 的 Go 链式对照测试；方法源从 request.Variables 读取 var1/var2/var3 行数，依赖方法源从 request.Dependencies 读取前一方法事件；Statement.Snapshot 对照 Java assertPropsPerRowIteratorAnyOrder。新增 3 个 Java runtime 已登记到 case.epl-fromclausemethod-basic 与 epl.fromclausemethod。Java TestSuiteEPLFromClauseMethod 3/3 通过；NW-twice、outer joins、cache、context、multikey/array-key、performance 与完整 Java 诊断仍为后续切片；本轮不需要 MySQL Docker。

本轮继续对照 Java `InfraUpdate`/`InfraNWTableFAF` 的失败路径：`ExecuteFireAndForget` 在 Table 或 Named Window mutation 中途失败，以及 pending routed event processing 返回错误时，恢复目标存储、Context 分区状态、Context Table row ownership 和待派发队列；新增 `TestOnDemandTableMutationRollsBackAfterMidBatchFailure`、`TestOnDemandNamedWindowMutationRollsBackAfterAssignmentFailure`、`TestOnDemandMutationRollsBackAfterRoutedProcessingFailure` 与 `TestOnDemandContextTableMutationRollsBackAcrossPartitions`，覆盖 Table/Named Window 的跨行赋值失败、route cycle limit 失败及跨 category partition 的 primary-key collision。该回滚边界只保证当前 FAF 目标 mutation 的状态恢复，不等同于跨 statement、跨目标路由副作用、listener dispatch 或外部资源的完整事务；本轮不需要 MySQL Docker。
